# 更新日志

版本规范：`主版本.次版本.补丁`（迁移数据 . 新功能 . 修复）。每个版本号对应 [GitHub Release](https://github.com/nikonotnicotine/Uranus_Imessage) 标签。  
BUG 反馈与功能建议，请加入 QQ 群：**1125033956**。

---

## 1.3.4  2026-09-26

这一版两件事：内置代理整个拿掉；服务商源可以选 API 类型了。

- **删：内置代理。** 过一道本地代理让回复又慢又容易出岔子，想走代理的直接在系统里开
  Clash 之类的全局模式就行，比程序里再绕一层快。聊天、生图、Instagram、搜索、天气、
  链接预览、检查更新、云备份现在全部直连。老版本里的代理设置升级后自动作废，不用管，
  「控制台 → 代理」那一栏也没了。

- **新：服务商源分 API 类型 —— 自定义 / OpenAI / Google Gemini / Anthropic Claude。**
  侧栏「连接」旁边点「+」会先问是哪一种；选官方那三家会把官方地址预填好，只要贴密钥。
  建好以后在源的设置页（以及快速配置向导）里也能改类型，地址还是空的、或者还是预填的
  官方地址时会跟着换，自己填过的不动。
  - **老版本升上来的、已经配好的源一律是「自定义」**，行为和以前一个字都不差。
  - **中转站、自建反代选「自定义」**。中转站配成 Gemini / Claude 类型一般会报 404。
  - 「OpenAI」和「自定义」发出去的请求完全一样，只是预填了官方地址。
  - 「Google Gemini」打 Google 原生接口，安全过滤四项自动全关；生图、向量、拉模型列表
    也都走原生。
  - 「Anthropic Claude」打 Claude 原生接口。温度上限是 1（超了会夹到 1），「最大 token」
    填 0 时给 8192。**Claude 类型不能画图、听音、看视频、做向量**，这几项要挂在别的
    类型的模型上，选了会直接提示配置错误。

---

## 1.3.3  2026-09-26

这一版专治「生图一直报错，别的地方同一个模型却好好的」。六个独立的原因，全在我们这边。

> **关于 1.3.2：** GitHub 上那个 v1.3.2 发出去的时候，下面这些修复的代码还没推上去 ——
> 标签指着的是 1.3.1 的代码。点了更新、提示「更新到 v1.3.2」的，实际装的还是 1.3.1，
> 生图该报什么错照样报什么错。**这一版才真正带上这些修**，已经「更新到 1.3.2」的直接再
> 点一次更新就行。

- **修：出图 90 秒一到就被掐断，慢一点的渠道时灵时不灵。** gpt-image 系列走中转站实测要
  60~90 多秒，同一条渠道测试按钮 66 秒出了图，紧接着私聊里两次都正好在 90 秒被掐掉 ——
  这又是一种「测试能出、真聊天时不行」。超时放到 **180 秒**。

- **修：出图超时之后还会再发两次，最坏让对方看着「正在输入」等四五分钟。** 等满一整个超时
  说明上游在画、只是慢，再发一次多半照样慢，按次计费的渠道还会再扣一次钱。现在**我们自己的
  超时不再重试**；连不上、连接被掐断这类（请求压根没送到）照旧重试。

- **修：中转站拿 HTTP 200 装着报错返回时，只报一句「返回里没有图片（缺 data 数组）」。**
  实测同一个 `Unknown parameter: 'response_format'`，同一家中转站有时给 400、有时给 200。
  给 200 的那次下面那条「剥掉字段重发」根本没机会走。现在 200 里装着报错、又没有图的，和
  400 一样处理：认得出字段就剥掉重发，认不出就把原话报出来，并注明是 HTTP 200。

- **修：改了生图模型之后，「测试出图」立刻就好，私聊里还在拿旧模型撞同一个错。** 这就是
  「测试能出图、角色自己出图就不行」的真正原因，和入口无关 —— 出图的代码只有一份，四条路
  （测试按钮、`/image`、角色自己写的 `[Image:]`、Instagram）走的是同一个函数。差别在**用
  的是哪一份配置**：出图排在整轮的最后，一张图四五十秒、一轮几条气泡能跑好几分钟，而私聊
  那条路取的是**这一轮开头**解析出来的生图模型。于是你在面板上把模型换掉、或者把出错的那个
  关掉，测试按钮（每次现读配置）马上就对了，正在跑的那一轮却还捧着几分钟前的快照。现在出图
  前当场重新解析，改完立刻生效。

- **修：上游抖一下，这张图就彻底没了。** 出图原来**一次都不重试**，而聊天那条路早就有退避
  重试。所以中转站偶发一个 504、或者拿 404 转述「我连不上上游渠道」，在你那头就是一张图
  没发出来 —— 同一把密钥同一个模型名，在别的客户端里你失败了会手点一下重发，这边没这个机会。
  现在 429 / 5xx / 以及响应体里写明是渠道故障的 404 会自动重试两次（等 1.2s、5s）。

  **干净的 404 仍然不重试** —— 那是真的模型名写错或地址填错，重试一百次也一样。判据是响应
  体里有没有 `openai_error`、`bad_response_status_code`、「无可用渠道」这类话。

- **修：选了「竖屏 9:16」或「宽屏 16:9」反而出不了图。** 这两档原来发的是 `576x1024` /
  `1024x576`，**589,824 像素**，而 gpt-image 系列要求总像素在 655,360 以上 —— 低于下限直接
  400。也就是说这两个档位在官方模型上从来没能用过。现在改成 `768x1344` / `1344x768`
  （1,032,192 像素），过得了下限，也仍然是 64 的整数倍。

- **修：生图时不时 400「Unknown parameter: 'response_format'」。** 我们那几个可选字段
  （`response_format`、`negative_prompt`、`size`、`aspect_ratio`）不是所有上游都认，原来的
  赌法是「不认的会忽略掉」，而**官方 gpt-image 系列不忽略，直接把整个请求 400 掉**。一家
  中转站背后常常挂着好几条渠道，一次请求轮到哪条是随机的，于是严格的那条 400、宽松的那条
  正常出图。现在撞上这种 400 会把上游点名的那个字段剥掉重发（一轮剥一个，最多四轮），控制
  台上认这一行：「上游不认 response_format 这个字段，去掉它重发一次」。

  被点名的要是 `prompt`、`model` 这种必需字段，或者压根认不出字段名（比如内容审查那类
  400），**一次都不会重发** —— 那种情况重发只是白烧一次额度。

- 顺带：比例被上游剥掉之后，出图成功那行日志不再说「（要的是 9:16）」了 —— 那句话会让人
  以为尺寸发过去了、是模型没照做。

- 生图自检 20 项新增（媒体自检 218 → 238 项），其中一条专门钉住上面那个快照 bug。

- **Photon（Spectrum SDK）升到 12.10.1**（原来 12.8.0）。收发、附件、tapback、正在输入、
  投票、Digital Touch、群聊都没有能感觉到的变化，我们用到的接口一个没变；低版本 macOS 的
  那个数据库补丁照样打得上。
  - 12.10 起 SDK 不再自己接管 Ctrl+C / `systemctl stop`（原来它收尾超过 3 秒会替我们强退）。
    现在由我们自己兜底：收尾超过 8 秒还没完就强制退出，不会卡着不走。
  - 更新之后照常会 `npm install`，不用手动做什么。

---

## 1.3.1  2026-09-24

- **修：Digital Touch 里手画的涂鸦，识别出来只说「一个红色的涂鸦」，不说画了什么。** 给识图
  模型那句提示词原来只要两件事：是哪一种手势 + 什么颜色。可涂鸦是这几种里唯一内容不固定的
  —— 心跳就是心跳，涂鸦可能是爱心、猪头、一行字。于是画什么都只回「一个涂鸦」，角色只能回
  「画的什么烂玩意」。现在涂鸦必须说出画的是什么，字数上限也从 30 放到 60 字给它腾位置。

- **修：Digital Touch 取内容超时就直接放弃了。** 共享线路只有一条线路，而原来的写法是「每条
  线路试一次」—— 等于一次超时就彻底放弃。实测这是间歇性的：连发两条，第一条 7 秒取回来了，
  第二条直接超时。现在整轮重试一次（「内容是空的」不重试，那是服务端明确说了没有）。

- 投票自检 102 → 105 项。

---

## 1.3.0  2026-09-23

- **角色现在能给投票加选项了**（`[poll_add:选项文字]`）。iMessage 的投票任何参与者都能加
  选项 —— 你手机上那个「添加选项」按钮 —— 以前只有你能用，角色只能在给定的几个里挑。现在
  现有选项都不合它意的时候，它可以自己加一个上去，加完会收到一条列着全部选项的提示，想投
  再投。加的是这个对话里最近那个投票，你发起的、它自己发起的都行。

  重复的选项不加（加出来两个「炸鸡」谁也分不清该投哪个），满十个了不加，选项文字超过 60
  字截断。加完那张新的选项表会立刻存下来 —— 不存的话它紧接着写的 `[vote:E]` 会对不上任何
  东西。

  「投票」还是那**一个**开关，没有新开关要勾：认投票、投票、发起投票、加选项四件事共用一条
  连接和一份落盘，拆开只会让人多勾几次。本地 Mac 模式下加不了（和投票、发起投票一样）。

- **修：Digital Touch 和手写消息经常读不出内容。** 现象是角色回「这是什么/你给我画了个什么
  东西/看不出来」，控制台上对应这一行：「这条 Digital Touch 的内容没取回来」，前一行是
  `取不到这条气泡的内容：The operation was aborted due to timeout`。取那条气泡的图**用的是
  读几个字段的 6 秒超时**，而它要把一整张图传下来，经常传不完。改成 20 秒。

- **修：角色投票、加选项时，日志里那个投票的标题是空的**（「已在「」里投了【X】」）。Photon
  那边投票资源的标题本来就常常是空的，而角色**自己发起的**那些投票，标题只存在我们这份记录
  里 —— 你一「加选项」就被那个空标题洗掉了。顺带：标题真空着的时候，给模型那句提示现在整句
  不提注释，不再拼出 `注释是：""`（那比不提更糟，它会当成「对方发了个没标题的投票」去演）。

- 修：日志里投票和聊天背景那两行的前缀重了一遍（`[投票·投票·Nero]`）。

- 投票自检 85 → 102 项。

---

## 1.2.10  2026-09-23

- **修：「投票」开关到这个版本才真正生效过。** 1.2.8 加了投票，1.2.9 修了一次，但两版里
  「收」这一半都是坏的 —— 角色发得出投票，可你发起的投票、你投的票，它一个都收不到，重启
  也没用。控制台上认这一行：开着投票的角色连上之后应该有「[投票·角色名] 已连上，开始盯着
  投票」，1.2.9 及以前那行**从来不会出现**。

  收投票事件要另开一条连接，而决定「起不起这条连接」的那句代码把设置读错了（少了一对
  括号，读出来永远是「开关没开」），于是那条连接压根没建过。发投票走的是另一条路、每轮
  现读设置，所以它照样发得出去 —— 看着就像「能发不能收」。「聊天背景变了」那个开关是同
  一处毛病，一起修了。

  顺带把那句「已连上，开始盯着投票」挪到连接刚建好时就报（以前要等收到第一个投票事件才
  报）。原来那个位置让「连接没起」和「起好了正等着」在控制台上长得一模一样，都是一行都
  没有 —— 1.2.9 那次没修对，一半是因为它。

- 投票自检 83 → 85 项。

---

## 1.2.9  2026-09-23

- **修：刚打开「投票」开关，角色发得出投票，但你投的票它一个都看不见。** 打开开关保存设置
  之后，你手机上发起投票、或者在它发起的投票里投一票，它全都收不到 —— 只有重启桥接（或者
  重启整个后端）才开始生效。控制台上能看出来：保存设置那行写的是「连接已对齐配置：保持 3」，
  而下面**没有**「[投票] 已连上，开始盯着投票」那一句。

  投票要另开一条连接去收事件，而它原来只在号码上线那一刻按开关起一次。判断「这个号要不要
  重连」时只看连不连得上的那几项（模式、项目号、密钥），投票开关不在里面 —— 所以保存设置
  时这个号被判成「保持」，那条收事件的连接压根没起。发投票是每次现读设置的，所以它照样发
  得出去，看着就像「能发不能收」。现在保存设置时会把这条连接对齐一次：开了就起、关了就停，
  不用再重启。**关**开关同理 —— 以前关掉之后那条连接还挂着替你收事件，要等重启才断。

  「聊天背景变了」那个开关是同一个毛病，一起修了。

  （这一条只修对了一半：开关本身还被读错着，所以这次改动当时看不出任何效果 —— 见 1.2.10。）

- **修：它自己发起的投票，你投了它说不出你投的是哪个。** 只会说「{{user}}在你发起的投票里
  投了一票」，说不出是【B炸鸡】还是【C海底捞】。发出去那一刻拿不到每个选项的内部标识（只能
  先留空），而「有人投票了」这个事件里只有一个标识、不带选项表，两边对不上。现在收到投票
  事件时会回头问一次这个投票的完整选项表，把标识补齐 —— 顺带它也就能投自己发起的那个投票了
  （以前得等你先投一票才补齐）。问不到的话照旧报「投了一票」，不会把这件事吞掉。

- 投票自检 78 → 83 项。

---

## 1.2.8  2026-09-23

- **新增「投票」**（「角色 → 投票」，**默认关**，只支持云端 Photon 模式）。三件事一起来：

  **一、你发起的投票，它看得见全部选项。** 以前你在 iMessage 里发一个投票，角色那边只
  收到你那行标题（「今晚我要吃什么」），选项一个都没有 —— 因为底层 SDK 压根不把
  「有人发起了投票」这种变化往下传，只传「有人投了票」。现在角色收到的是一整句：

  ```
  [系统提示:{{user}}向你发起了一个投票，注释是："今晚吃什么"，选项有【A麻辣烫】【B炸鸡】【C海底捞】。
  你可以选择投票，格式：[vote:A]。一次只能投一个选项。
  你也可以不投票，直接回复文字即可]
  ```

  **选项随时能加。** 你给投票加第四个选项，角色会重新收到**全部四个**（【A】到【D】），
  不是只收到新加那一个。

  **二、它能真去投一票。** 模型写 `[vote:A]`，你手机上那个投票里就会多一票。也认选项
  原文（`[vote:麻辣烫]`）和序号（`[vote:2]`）。**一次只能投一个** —— 苹果的投票就是一人
  一票，再投一次等于把上一票挪过去，所以提示词里也只教单选。

  **三、它能自己发起一个。** 写 `[poll:今晚吃什么|麻辣烫|炸鸡|海底捞]`，你手机上会收到
  一个真的投票气泡。最少两个选项、最多十个，超了截断。你在里面投了票，角色会收到
  「{{user}}在你发起的投票里投了【B炸鸡】」。

  它**自己发起**的那个投票它自己投不了：刚发出去时拿不到每个选项的内部标识，得等你先
  投一票才补齐。

  顺手修掉了日志里那条 `[spectrum.imessage.poll] failed to cache poll` 的报错
  （`path:["title"] Too small: expected string to have >=1 characters`）—— 投票的标题
  是作为**另一条普通文本消息**单独进来的，所以那条投票事件里标题常常是空的，SDK 拿它
  去校验就炸了。现在标题说不清就回源问一次权威的那份，那条报错不再出现。发起投票时
  重复的那行标题也压掉了，模型不会看到两遍。

  **默认关的理由**和聊天背景同一条：这条事件不在主连接上，开着就意味着后台多起一条
  只订阅「投票」的 Photon 连接。本地 Mac 模式下开关开着也不起作用，发起投票会退化成
  发一句「今晚吃什么：麻辣烫 / 炸鸡 / 海底捞」。

- **新增「看手写消息和 Digital Touch 的内容」**（「角色 → 手写和 Digital Touch」，
  **默认关**，只支持云端模式，要先配好识图）。

  这两种气泡**本来就认得出**（模型会收到「{{user}}发来了一条手写消息」），缺的是里面
  是什么。现在把那条气泡的内容取回来送去识图，模型看到的是
  `手写消息内容：今晚一起吃饭吗`。

  **手写消息尤其值得开** —— 那几个字是对方真正说的话，以前一个字都读不到。
  Digital Touch 则会说清是哪一种（心跳、火球、亲吻、心碎……）和主要颜色。

  用的是专门的提示词而不是通用那句「描述这张图片」：拿通用那句去看手写，模型会答
  「一张白底的蓝色手写字迹」，而不是把那句话读出来。

  取不回来、或者取回来不是图片，就还是原来那句「发来了一条手写消息」，不会更糟。
  默认关的理由和识图一样：每条这种消息都要打一次识图模型，那是花钱的。
  关着的时候**压根不去取**。

  **角色画不了心回过去** —— 底层没有发手写 / Digital Touch 的口子，这条发不了。

- **修：群聊被当成了某个人。** 归一「这条会话对面是谁」时只看末段，`iMessage;+;chat123456`
  的末段滤掉非数字剩 `123456`，六位，正好够长 —— 于是一整个群被当成了一个手机号。
  受影响的是「聊天背景变了」那条提示（群里换背景会记到一个不存在的「人」头上）。
  现在按分隔符判单聊/群聊，和 SDK 自己的判法一致。

- **修：点了「结束线下」之后还在线下模式里待好几分钟。** 收尾要打两次模型（补一份小总结 +
  出一份大总结），慢的时候好几分钟；而那个「线下开着吗」的开关原来是等这两步都跑完才按掉的。
  这几分钟里你发的话**全进了剧情**，主动消息还被压着，而你以为已经回线上了。

  网页那边更难看：反代一般 60 秒就把这条请求掐了，前端拿不到新状态，页面上一直挂着
  「线下开着」，怎么刷都不变（因为后端也确实还开着）。

  现在**第一件事就是关掉线下**，再去出总结 —— 总结是「这段剧情记点什么」，和「现在还在不在
  线下」本来就是两件事。页面上那三步的顺序也照实改了，并写明第 2、3 步不用守着。
  那条请求真被掐了的话，现在会去重新读一次状态，告诉你「线下已经关了，总结可能还在后台跑」。

- **修：关了线下，排在队列里的那几轮还在按线下演。** 你在手机上连发三条、然后在网页上按
  「结束线下」——入口那道闸是在入队之前，所以剩下两条早就过了闸，接着按线下预设生成、
  接着发四条选项。现在生成前再问一次「线下还开着吗」，关了就把这一轮挡下（那句话也不进剧情），
  并且明确告诉你「线下模式已经关了，这句没进剧情」，而不是含糊报一句「这轮没回上来」
  让你以为模型挂了、一直重发。

- 自检 99 → 179 项（新增 `scripts/test-poll.mjs` 80 项），线下那套自检 206 → 248 项。

---

## 1.2.7  2026-09-23

- **新增「一直没收款就提醒角色」**（「角色 → 转账卡片」，**默认关**，开着「贴表情就算收款」
  才看得到）。开了默认等 **120 分钟**，能改成 1 到 1440（一整天）之间任意一个数。

  转出去那笔过了这么久还没被你收，角色就会收到一句「你转的钱还没被领取」，
  它自己决定要不要问一句。

  **只提醒一次。** 不会隔一阵催一遍 —— 关心和讨债差的就是第二遍。这个「提醒过了」
  是记在硬盘上的，不是记在定时器里：中间重启多少次，那笔也还是只被提醒那一次。

  收了款、或者你中途把这个开关关了，那笔就不提醒了。半夜（勿扰时段）、开着提示词
  协助模式、开着线下模式的时候都不会冒出来 —— 往后推，等能说话了再说，那一次不会白掉。

- 自检 89 → 99 项。

---

## 1.2.6  2026-09-23

- **小图标那档的 logo 回到正中间了。** 1.2.4 把留白做成偏的（上少下多），想给卡片上那行
  浮字腾一条空带子；真机上看下来 logo 只是偏上（中心落在 34.6% 而不是 50%），而那行字并
  没有真把 logo 盖掉 —— 横幅那档从一开始就是对称的、那行字同样压在图上，一直没人觉得
  有问题。

  **logo 尺寸一点没变**，留白框还是那么高，只是挪回中间。

- **又多了七张自带的支付 logo**：Apple Pay、PayPal、Venmo、Visa、Zelle、微信支付（方块版）、
  支付宝 —— 连原来的 Chase 和微信支付横版一共九张。

  **新建**的角色出厂就选好 Apple Pay + 小图标。已经存在的角色一张图都不动。

- **修：一张 SVG 能把整个服务打死。** 图里写的宽高是负数（或者大得离谱）时，渲图那个库
  会在原生层直接 abort —— 不是报错，是进程当场没了，什么都拦不住。自带的那张 Venmo
  官方 svg 就写着 `height="-1503"`，所以这一版加它的时候撞上了。

  现在这种图先按它自己的 `viewBox` 重算一对尺寸再去渲。顺手治好了另一类：图标库里那种
  只写 `viewBox="0 0 24 24"` 的小图标，以前会缩成画布中间一个小点，现在正常铺满。

  自己传图的人**都可能**撞上这个 —— 从设计软件里导出的 svg 什么数值都有。

- **新增「收款后立刻通知角色」**（「角色 → 转账卡片」，**默认关**，开着「贴表情就算收款」
  才看得到）。

  | | 你一收款 |
  |---|---|
  | 关（默认） | 只记一句「你已收款」，等你**下次发消息**时跟着一起送进去 |
  | 开 | 角色**当场**就回一句（「钱收到了吧」那种） |

  默认关和「贴 tapback 不当场回」是同一条理由：贴个 emoji 就把角色勾出来说话有点太轻了。
  而且收完款你多半自己就接着打字了，那时候攒着的那句提示正好跟着你的话一起进去。

  开着的时候走的是发送队列而不是直接开一轮 —— 你贴完表情紧接着来一句「收到啦」，
  两件事会并成同一轮，角色只回一次。

- 自检 82 → 89 项。

---

## 1.2.5  2026-09-23

- **修：没填「气泡上方那行署名」的话，转账卡片一张都发不出去。** 空着的时候整条请求
  被服务端打回 `app_name must not be empty`，卡片退化成一句「转账 $1.00 备注」的纯文字。
  1.2.2 起就这样了（那版把「留空 = 那行署名不要」做成了原样交一个空串）——
  可**协议上压根表达不了「那行不要」**，这个字段不许为空。现在空着一律用「转账」，
  和图上那行字共用同一个词。

  填过署名的用户不受影响，这三版里一直是好的。

  没早点发现是因为假的上游不校验这个字段：离线自测一路放行，只有真机发的时候才报。
  现在那条断言盯着交上去的值本身，不再只看卡片的排版。

- 界面上那行提示跟着改了：以前写「留空就不显示那行」，照着填空的人只会得到一句纯文字。

- 自检 80 → 82 项。

---

## 1.2.4  2026-09-23

- **缩略图可以显示成小图标了**（「角色 → 转账卡片 → 显示得多大」）。两档：

  | 档位 | 那条带子 |
  |---|---|
  | 横幅（默认） | 占满一条，600×300 —— 就是 1.2.3 的样子 |
  | 小图标 | 压扁成窄条，600×156，同一张 logo 看着小得多 |

  为什么是「画布多高」而不是「logo 多大」：那张图在气泡里的位置和尺寸**全由苹果定**
  （按气泡宽度铺满，高度跟着图的比例走），我们既不能说「画小一点」也不能说「放右边」。
  唯一的杠杆就是画布压多扁。

  小图标那档**留白是偏的**（上少下多），因为卡片上那行浮字压在图的下边缘 ——
  横幅那档图高 300，那行字落在下缘碍不着谁；压到 156 之后不留出来会直接盖在 logo 上。
  那条空带子的高度是估的，真机上那行字偏高偏低都有可能。

  默认还是横幅，**已经配好的卡片一张都不变**。

- **界面上「卡片左边那张图」说错了**：那张图在金额那三行字**上面**，不在左边。

- 界面上那几个预览格子现在跟着档位变形状 —— 一直画 2:1 而发出去是 3.85:1 的窄条，
  等于在界面上骗人。顺带修掉一个预览溢出：留白给到 `<img>` 上不生效（它是替换元素，
  高度按固有尺寸算，底部那条留白被忽略），图会顶出画布。

- 自检 76 → 80 项。

---

## 1.2.3  2026-09-23

- **转账卡片可以带一张缩略图了**（「角色 → 转账卡片 → 卡片缩略图」）。**可以不选** ——
  不选就还是原来那张纯文字卡片。随程序自带 Chase 和微信支付两张，也能自己传
  （SVG / PNG / JPG / WebP，单张 2MB 以内），或者往 `data/transfer-logos/` 里丢文件
  再回界面点一下「重新读取」。

  **SVG 直接收**，不用你先转成 PNG —— 品牌 logo 基本都是这个格式。

  不管原图是方的还是长条的，服务端都会等比缩放居中放到一块 600×300 的画布上，
  四周留白填你指定的底色（默认白）。所以换张 logo 卡片不会忽然变高变矮，
  Chase 那种 5 比 1 的长条也不会被裁得只剩中间几个字母。

- **「卡片上那行小字」其实是气泡上方那行署名** —— 界面上的名字和说明都写错了，现在改了。
  你填「Chase」之后真机上出现的是气泡上方那行「某某发送了 Chase 信息」，不是卡片里面。
  卡片里面从来只有金额、备注、状态三样，那是苹果定的版式，我们排不了。

  留空还是「那行整个不要」，和上一版一样。

- 新增依赖 `@napi-rs/canvas`（渲染那张缩略图用的 skia 绑定）。顺带修掉
  `package-lock.json` 里两处对不上的地方：`server` 那一栏压根没声明这个包
  （clone 下来只是**碰巧**因为别的包也依赖它才能用），以及还躺着一个早就不用的
  `concurrently`。

- 自检 53 → 76 项。

---

## 1.2.2  2026-09-23

- **转账卡片的货币符号可以自己填了**（「角色 → 转账卡片 → 货币符号」）。原来金额前面
  那个 `￥` 是写死的，没有任何技术理由 —— 就是当初只想着一种钱。填 `$` / `€` / `HK$`
  都行，写「金币」「点券」也随你（最多 4 个字），留空还是 `￥`。

  顺带修掉一处：认金额时剔符号那一刀原来照的是一张写死的清单，只认 `￥¥$`。你填了
  `€`、角色跟着写「€4000」的话，那笔 4000 会显示成「€€4000」。

- **卡片底下那行来源小字可以不显示了**。「卡片上那行小字」清空就没那行，只剩金额、
  备注、状态三样。原来空着会自动变成「转账」，所以「我不要那行字」压根没法表达。

- **改「已收款」时那张卡片有几率改不动**（而且是悄悄失败，用户只看到「贴了表情没反应」）。
  转账落盘的字段清单里漏了卡片上那行小字，改卡片时读回来是空的。以前那行空着会兜底成
  「转账」、发和改两边正好兜成同一个，所以一直没露出来；这一版允许留空之后就会变成
  「发的时候一个名字、改的时候另一个名字」，在 iMessage 那儿等于「另一个 app 想来改
  这张卡片」，直接被拒。

  这两个字段现在都**跟着每一笔一起存**：你中途去设置里把符号从 `￥` 改成 `$`，一张
  老卡片收款时也不会当场把数字换个意思。

- 自检 48 → 53 项。

> 上一版留的那个问号（共享线路换上 Spectrum 那个扩展的身份之后到底放不放行）已经在真机
> 上验过了：**放行**，卡片发得出去。点开那张卡片会落在 Spectrum 里 —— 骑别人的扩展就是
> 这个样子，不是哪儿坏了。

---

## 1.2.1  2026-09-23

- **转账卡片一张都发不出去**（1.1.9 刚加进来的那个功能）。报错是
  `Only the Spectrum mini app extension is allowed on this endpoint`，每一次都失败，
  不是偶发。

  卡片上那个「谁发的」身份原来填的是一对明显不属于任何人的**假值**，当时的想法是
  「服务端只验格式、不验归属，所以填谁的都不对，那就谁的都不填」。这句话在共享线路上
  是错的 —— 它验的是**相等**：整条线路只放行 Spectrum 自己那个 iMessage 扩展，填别的
  一律在请求发出去之前就被拒掉。官方文档里没写这条。

  现在骑的就是那个扩展（Spectrum Mini App Hub，苹果审核过的启动器，本来就是给没有
  自带扩展的调用方用的）。**卡片长什么样一点没变** —— 金额、备注、「待收款」那几行字
  来自另一套字段，跟身份无关。变的是两件小事：卡片上那行来源小字在**装了 Spectrum
  的人**那儿可能显示成 Spectrum 的名字和图标；以及**那张卡片从此是能点开的**（装了就
  跳 Spectrum，没装就被引导去 App Store）—— 以前点了没反应。界面上那句「这张卡片
  点不开」跟着改掉了。

  点开落到哪也跟着换了：以前指向一个代码仓库（反正点不开，填什么都无所谓），
  现在指向发出这张卡片的那个服务的官网。

  仿 Apple Cash 的路**试过，走不通**，而且原因和之前以为的不一样：它不是「没有接口
  能发」（其实和我们走的是同一套机制），是那种气泡的**画面由 iOS 自己画**，我们填进去
  的金额和备注它压根不看。

- **一整轮回复只有一张转账卡片、而卡片没发出去时，那一轮会整个丢掉**。对方收到的是
  一句「这条没能发出来」，这笔钱的意思一个字都没送出去。

  现在退化成当普通文字发一句「转账 ￥4,000.00 零花钱」—— 丢了卡片那层样子，但话说
  出去了。本地 Mac 模式（压根没有这条通道）早就是这么做的，云端这条是漏了一处，
  两边现在一致。

- 自检 46 → 48 项。原来有一条专门盯着「身份必须是假值」，正是它把错的前提钉住了；
  换成盯「必须是 Spectrum 那一套、而且发和改两次完全一致」，防冒充那层意思留着 ——
  换成任何一家真金融机构或社交 app 的身份照样红。另加一条盯那两条文字退路都在。

> 发布时还不确定共享线路换上这份身份到底放不放行（那句报错只点了扩展 id 一个字段，
> 服务端会不会连 team id 一起卡不清楚）。已经在真机上验过了：**放行**，卡片发得出去，
> 点开落在 Spectrum 里。

---

## 1.2.0  2026-09-23

这一版全在**出网代理**上。起因是用户的两句话：「为什么我这边显示 API 也要开代理啊，
我 API 在 cherry 不开梯子都能直接用，你逗我笑呢？」和「能不能就写开了才用代理，
不开直接用国内多直连啊？很恼火啊这样」。两句都是对的，下面三件事各对应一句。

- **代理没开的时候，勾了代理的类别会自己脱开代理直连再试一次**。

  以前勾了代理就是「只走代理」：Clash 没启动、换了端口、隧道被掐，这几类就直接失败 ——
  哪怕目标（天气、中转站、GitHub、音乐）在国内本来直连就通。为了一个没开的客户端
  把本来能成的事搞失败，说不过去。

  新的 `fetchVia` 统一做这件事，全部 30 处出网调用点都换过去了。报错里还会缀一句
  「脱开代理直连也不行：<错误码>」—— 省掉「那我关了代理再试试」这一轮。

  **只做单向**（代理挂了 → 直连）。反过来「没勾代理、直连失败了就偷偷试代理」刻意不做：
  那会让模型 API 在上游抖一下的时候被悄悄换成代理的出口 IP，可能触发中转站风控 ——
  而那种坏法压根联想不到是代理干的。

  **超时不算「代理坏了」**，不触发回退：已经白等了一整轮，再等一轮只会让这件事更慢，
  而且超时既可能是代理不通、也可能是目标本来就连不上。

- **报错不再一律让你去开代理**。这就是「API 也要开代理」那句提示的来源，是个实打实的 bug。

  那句话原来只分「走着代理 / 直连」两档，于是**任何**一类直连失败都建议去勾代理，
  包括模型 API —— 而同一个文件的注释里明明写着中转站直连就通、绕代理反而慢还可能被风控。
  两句话自相矛盾，用户照着提示去装一个压根不需要的梯子，真正的原因（这台机器出不了网、
  接口地址填错）被那句话盖住了。

  现在每一类多了一个「是不是真被墙」的标记：模型 API、音乐、Photon 这三类失败时，
  那句话只指向「这台机器能不能出网、接口地址填对没有」，一个字不提代理。

- **出厂勾选按实测改了**：天气**取消**默认勾选，联网搜索**改成**默认勾上。

  在用户机器上把十类逐个真打了一遍（环境变量里的代理全清掉）：三家天气源直连全通
  （open-meteo 1.1s、和风 250ms、WeatherAPI 725ms），而 DuckDuckGo 和 Brave 都吃满
  8 秒超时。所以天气那个勾一直是白绕一道，搜索那个才是真需要的。

  天气那处一直没人发现，是因为 `env.js` 自己带了一段私有的直连回退把它兜住了 ——
  那段现在归 `fetchVia` 统一做。

  **老用户自己改过的勾选不动**：显式存下来的值优先，新的默认值只填空缺。

- **修了一个会让代理配置整份失效的 bug**。`proxy.js` 里用到了一个没 import 的变量，
  抛出来的 `ReferenceError` 被外层的 try/catch 吞掉 —— 结果是**你填的地址和勾选被静默
  丢弃、退回出厂默认**，界面上看不出任何异常。新装时、或者恢复一份不带密钥的搬家包之后触发。

- 自检从 119 项加到 213 项。除了把新行为钉住，还多了两条防复发的：业务文件里不许再出现
  裸的旧写法（那个写法照样走代理、只是少了直连那一刀，唯一的症状是「代理没开时这个功能
  失败，别的都好」，没人查得出来）；以及**真跑一遍** —— 起一个本机服务当目标、把代理指到
  一个永远连不上的端口，请求必须靠直连那一刀拿到 200。

---

## 1.1.6  2026-09-20

- **发视频像卡死了一样**。根子是那段字节是在消息循环里**同步**下载的：一段 15MB 要几十秒，
  这期间这条线路后来的消息全堵在 SDK 缓冲里不动。
  - **角色没开「看视频」时压根不下载了**。以前不管开关都先把字节读进内存，然后
    `describeVideos` 一看没开就整段扔掉 —— 那几十秒的卡顿换回来的只是一句写死的
    「你看不到内容」。现在入站先看开关，关着就直接把那句话塞进队列。
  - 顺带解释了另一个观感：「单发不行、还得再发一条才回复」。后发那条不是**触发**了回复，
    是**排在下载后面**——视频那轮本来也会回，只是慢。
  - 下载**本身**还是在消息循环里（这轮没动并发模型）。关着开关时的卡顿现在整个没了，
    开着时该等的还是要等，但有日志说明在等什么。

- **控制台看不出「现在在干什么」**。每个耗时步骤以前只在**结束**时吭一声，而好几个「结束」
  还只在 DEBUG 那一档（默认看不见）—— 于是一轮里最长的那几段等待在控制台里是纯空白，
  和卡死分不出来。现在七处都配上了「开始…完成（花了多久）」：
  - **下载附件**：以前只在**重试**时才出声，一次顺利的 15MB 下载从头到尾一个字都没有。
    现在前后各一行，≥2MB 走 info，更小的走 debug（免得每张表情包都刷一行）。
  - **打聊天模型**（`CHAT_TIMEOUT` 300 秒，一轮里最长的一段）：「正在等回复」从 debug
    提到 info，完成和失败两条路都带上等了多久 ——「等 3 秒就报错」和「等满 300 秒超时」
    是两种毛病。
  - **识图 / 听语音 / 看视频**：各加一行「开始识别…」，视频那条还报体积，完成带耗时。
  - **后台总结记忆 / 备忘录**：加一行「开始在后台跑…（不影响这一轮回复）」。它确实不拖慢
    这一轮，但以前没有开始那行，用户只能猜「是不是后台在总结」，而这个猜测既证实不了
    也排除不了。
  - **打字停顿发气泡**：从 debug 提到 info，并且先把总时长算出来报掉（「预计 42s 发完」）
    —— 那段静默是**故意**等的，不是故障。
  - **生图 / 语音合成**：各加一行「开始…」。两样都是同步等的，以前只有成功那行日志。

- **黑框窗口那份日志现在带时间**（`HH:MM:SS`）。网页控制台本来就有，`.bat` 窗口这份以前
  只有 `[来源] 内容` —— 从黑框里拷出来的日志是一段没有时间轴的文本，「这两行之间隔了多久」
  「这条是十分钟前的还是刚才的」全看不出来，而排查时那正是最要紧的信息。不带日期也不带
  毫秒（要毫秒看网页控制台），格式和网页那份的前半段一致，两边对照时不用换算。

- **Instagram 的互动进了上下文，但没带帖子内容**：上下文里只有一句「niki 发了一条新帖子，
  你刷到了」，发了**什么**一个字都没有。于是几轮之后聊到这条帖子，模型手里只有「他发过帖」
  这个事实，内容全靠编。
  - 现在 `mark` 里**配文和画面两样都带**（不是二选一）—— 它们是两件不同的事：配文写
    「终于」、图是一张病历单，只带配文的话模型下一轮照样接不上话。
  - 画面那份按 `visionNote`（识图模型看着真图写的）→ `alt`（生图时那句提示词）退，和
    `igprompt.js:mediaText` 同一个优先级。**这条退路是必需的**：点赞那条路在
    `ensureVisionNote` 之前就返回了，第一个刷到的角色手上 `visionNote` 还是空的。
  - 三处调用点都带上了，**包括点赞那条**（`likeChance` 默认 45，最常走的一条）——
    漏了它，用户发的近一半帖子在上下文里都只剩「他发过帖」。
  - 两样都没有时明说「没写配文也没有图」。留白会让模型当成信息缺失，然后自己编一个。
  - 配文截 80 字、画面截 60：`mark` 每轮都跟着上下文走，长了会挤掉真正的对话。

---

## 1.1.5  2026-09-20

- **备忘录被一句审核提示语整份覆盖，备份也跟着没了**。上游回的是 **HTTP 200，正文就是
  那句 `The prompt could not be submitted. The prompt contains sensitive words that
  violate Google's Generative AI Prohibited Use policy...`**，于是它被当成一份合格的
  备忘录写了进去；第二次总结时 `writeMemo` 又把这份坏的复制进 `.bak`，原来那份彻底没了。
  - **两道闸同时漏**，这是实测出来的不是推断：`isContentBlocked` 开头只认 400/403/422/451，
    200 直接放过；`looksLikeRefusal` 判的是道歉措辞，而那句话没说「抱歉」也没说「我无法」，
    主语压根不是模型。
  - `looksLikeRefusal` 现在直接拿 `BLOCK_PATTERNS` 判正文 —— 400 那条路认的措辞，
    200 的正文里出现同一句也得认。末尾那道闸的条件从 `refusalTries`（循环里累加的）
    放宽到 `refusalAllowed > 0`，否则「压根没判出来」那种情况会从闸底下溜过去。
  - **换 3 次说法都被拦，就当这一轮没有生成**：待总结一个字节不删、备忘录一个字不覆盖，
    控制台记一条 error，聊天窗口当场发一条说明（「换了 3 次说法都没过去，你现在看到的
    还是上一次那份」）。内容安全这一类**第一次就说**、不攒到 `maxFails` —— 它不会自己
    好转，而用户必须当场知道这一轮没更新。说过一次就把计数归零，不刷屏。
  - 另加一道缩水闸：模型正常回了、但把旧条目漏掉一半（8 条缩到 3 条）也不覆盖，抛错并把
    这一版全文打进控制台日志。和上面那条是互补的两件事。
  - 已经被覆盖掉的那份救不回来（`.bak` 只有一份，第二次生成时被顶掉了）。

- **上下文页面看不到新消息**。磁盘上一直是写着的 —— 「刷新」按钮只刷左边那列，右边详情
  的 effect 只认 `itemId`，会话没换就不重新拉。打开一条会话、在手机上又聊几轮、点刷新，
  看到的还是打开那一刻的快照。现在刷新按钮两侧一起刷，另外每 10 秒自动拉一次（正在编辑
  或有请求在跑时暂停，否则会把你打字的内容拉回旧版本、`commitEdit` 还可能按下标改错条）。

- **Instagram 上的互动不进上下文和记忆库**：角色明明看到了、点了赞，私聊里却跟个没事人
  一样。两条最常走的路以前都是直接 return、什么都不记。
  - **点赞**（`likeChance` 默认 45）：现在照样写一轮「你刷到了什么 + 你点了个赞」。
  - **看到留言没回**（`replyChance` 默认 60 没中）：语义是「看到了，没回」而不是「没看到」，
    所以也记一笔，`mark` 里带上你那句原话（超 60 字截断）—— 下一轮私聊里角色才接得上话。
    丢的是**用户主动说的一句话**，比点赞那条更要紧。IG 上不留痕迹，没回就是没回。
  - 两条合并成一个 `recordSeen`，共用「自己拼 vars、现取会话、过 `recordPeer` 闸、
    吞掉 commit 异常」那套 —— 各写一遍迟早漂移成「点赞记了、看到没回没记」。
  - 三样（记忆 / 备忘录 / 日记流水）是 `recordTurn` 一次写的、共用同一份原文和同一个
    时间戳，所以不存在「记忆记了但日记没记」。缺的从来不是这条链，是压根没走到它的出口。
  - 角色间的点赞照 `recordPeer` 那道闸走，和评论轮同一个规矩（默认开着）。
  - `commitIgTurn` 多一道防护：`mark` 是空的就记一条 warn 并跳过，不往存档里塞空的
    user 消息（下一轮拼提示词就是个空 turn，有的上游直接回 400）。
  - **待总结里攒着的原文不注入提示词**，这是分层不是漏：注入的是总结**之后**的产物
    （近期记忆 / 过往回忆 / 备忘录）。原文比总结大一两个数量级，而 IG 每条帖子每个角色
    都要跑一轮。日记同样不注入 IG 那一轮，理由和原来一样。

---

## 1.1.4  2026-09-20

- **Instagram 整个分区不动了：角色不回评论、不点赞、彼此也不互动**。根子是每条互动
  任务都必然吃一个上游 400 `Requests ending with a model turn are not supported.`，
  而队列是**先出队再执行、失败不重试**的，所以跑一条丢一条 —— 队列被清空，`/立即触发评论`
  敲下去只会说「队列里现在没有排着的互动」。
  - **口径不一致**。我们两边都只看消息数组字面上的最后一条是不是 `assistant`，而上游判的是
    **contents** 以 model turn 结尾 —— 中转站把所有 `system` 提走当 `systemInstruction`，
    它压根不在 contents 里。所以 `[…, assistant, system]` 在我们眼里合法，在 Gemini
    3.7/3.8 眼里正是它拒收的那个形状。
  - **为什么只有 IG 撞得上**：聊天那一轮末尾永远是刚进来的那条用户消息。IG 这一轮不由消息
    触发，末尾那条「现在轮到你了」的行动指令挂的是 `system`，而从存档恢复的上文最后一轮
    十有八九是角色说的话 —— 一撞就是这个形状，而且是常态不是边角。
  - **两层都补上同一条口径**。`igprompt.js` 里行动指令的判据从「数组里有没有 user」改成
    「跳过末尾的 system 之后，最后那条是谁说的」，从源头不产出这种形状；`llm.js` 的
    `moveModelTail` 先把尾部的 system 摘下来搁一边、找到 assistant 尾巴挪完再放回去，
    给所有链路兜底（不只 IG）。上文正好以用户那句话结尾时一个字不动，模型看惯的形状不白改。
  - **测试反而把 bug 钉住了**：原来那两条断言期望的末尾正是 `assistant, system`，所以
    487 项全绿也照样漏。换掉，另补四条 —— IG 侧验上文以 assistant / 以 user 结尾两种形态，
    llm 侧验末尾挂 system 时的挪移、以及不该挪时不动。
  - 已经丢掉的那些任务补不回来（队列里没有记录了）。修完之后新排进去的会正常跑。

- **线上默认预设：开箱只开六条固定条目**，12 条 custom 一律默认关。新用户首次启动会把
  内置种子拷进 `data/presets/` 并自动选中第一份，以前拿到的是一份 custom 全开的预设 ——
  角色定义、自我认知、禁止催促、禁用词库、活人感增强、代词校准、尊重用户、情绪引导、
  紧跟时事、思维链、防掉格式、卡思维链（预填）。
  - 那些是攒出来的调校，不该是开箱默认：新用户还没有任何参照，就先吃了一整套别人的口味。
    其中「卡思维链（预填）」在 Gemini 3.7/3.8 上压根不支持（就是上面那个 400 的来源）。
  - 现在默认开着的是 `char` / `user` / `消息格式与功能` / `上下文` / `记忆库`，和代码
    兜底（`makeDefaultPreset`）一致。世界书照旧关着 —— 新用户一本世界书都没有。
  - **12 条 custom 的正文、名字、顺序一个字没动**，只是开关拨到关，要哪条自己去开。
    生成参数和那 5 条去思维链的正则也没碰。
  - 只影响新装的：种子只在 `data/presets/` 压根不存在时才拷，已有目录一律当老用户处理。

---

## 1.1.3  2026-09-20

> 1.1.2 那个版本号作废了，别去拉它 —— 它的 tag 建在了上一版的提交上，
> 内容其实还是 1.1.1。更新器照 tag 下 tarball，所以会一路正常走完然后说
> 「文件内容和新版一模一样」。下面这些东西全在 1.1.3 里。

- **图片上限从 8MB 提到 20MB**。实机撞出来的：`图片 9.8MB，超过 8MB 上限`。iPhone
  随手一张就能过 8MB（实况照片、48MP 原图、HEIC 转出来的 JPEG），那道闸不是在防异常
  情况，是在拒正常照片。
  - 当初写 8MB 的理由是「再大 base64 之后请求体会顶到中转站的上限」—— 那句话把两条路
    搞混了。12mb 那个限制是 **express 的 body-parser**，管的是浏览器往后端传（试图那个
    按钮）；对方发来的图是**后端直接往中转站发**，压根不经过 body-parser。
  - 20MB 是照着语音、视频那两道闸对齐的，而且 `spy.js` / `spyphone.js` 收图早就是
    20MB —— 同一个仓库里同一件事只有这里卡在 8。
  - 顺手给图片也加上**超了压根不下载**（先看 SDK 报的体积），和视频一个待遇。

- **对方发来视频，角色能看见了**。以前视频连「收到了一个附件」都不算 —— 图片有识图、
  语音有听音，视频两边都不沾，走到最后那句「不认识的类型」就被丢掉，模型压根不知道
  发生过这件事。现在多了一个**看视频**模型分类，和听音同一条 Gemini 原生路径
  （`generateContent` + `inline_data`，所以同样只有 Gemini 系能用），转成一段描述再
  拼给聊天模型。**默认关**。
  - **SDK 里没有「视频」这种类型**。语音有专门的 `voice`，视频没有 —— 它就是一条普通
    附件，靠 `mimeType` 是不是 `video/` 开头认出来。不转码，原样发上去。
  - **一道 20MB 的闸**，而且**超了压根不下载**：先看 SDK 报的体积，报大了直接拒，那
    20MB 一个字节都不进内存。这个数是拿五家中转站实测定的 —— 12MB 是 4 家过 / 1 家
    网关 413，19MB 是 4 家过（最慢 69 秒），52MB 只剩 2 家。20MB 正好卡在「基本都能
    过」和「一半退货」中间。
  - **识别完或者报错都立刻把字节放掉**。base64 会把 20MB 涨成 27MB 的字符串，源
    Buffer 还活着的话峰值 47MB，再乘上同时在聊的会话数。所以每条视频带一个
    `release()`，**七条退出路各钉一遍**：成功、单条出错、功能没开、超过上限被切掉的、
    那一轮提前退出的两处、桥接已经停了、以及停连接时丢掉的排队槽位。
    「功能没开」那条尤其要紧 —— 取字节发生在检查角色开关**之前**（和图片、语音一致），
    漏了它就变成「关掉比开着更费内存」。
  - 一轮里超过上限（**默认 1**，可调到 5）的视频会被跳过。默认给 1 不是 2：一段视频的
    请求体比一条语音大一个量级。真读不到或者太大时，会往那一轮里塞一句「对方发来一段
    视频但你看不到内容」—— 和图片读不到时同一个待遇，「没看见」和「看见了看不清」是
    两种回法。
  - 「连接」面板里挂了「看视频」分类的模型有个**「传视频试试」**按钮，走的是和真实链路
    一模一样的路。本地放宽到 32MB（而不是 20MB）——想探自己中转站的天花板，得允许超过
    闸门试；成功和失败都会报体积和耗时，因为 413 能拿到的线索只有这两个。
  - 新增 `scripts/test-video.mjs`：68 项离线自检，专门盯「超了不下载」和 release 那七条
    退出路（写完拿注掉 `finally`、废掉体积闸两种改法验过，各自都真的挂了测试）。

- **对方发来一条链接，不再只是一串看不懂的字符**。iMessage 里发网址，手机上渲染成一张
  带标题封面的卡片，但那张卡片是对方手机自己抓的，**元数据一个字都不会传过来** ——
  到我们这儿正文就是那串网址本身。普通链接还好，模型多少猜得出；**短链**就彻底瞎了。
  实机日志里那条 `https://163cn.tv/bgIOj7V8`，模型回的是「163在这边打不开…是什么
  song？」；另一条 b23.tv 更糟，模型自己写了 `[搜索:https://b23.tv/KRCChOl]` 想补救，
  对一条短链当然搜不出东西，白花一次搜索加一次生成。
  - 靠 **Open Graph** 拿标题和简介，不给每个 app 单独写一套 —— iMessage 自己抓卡片靠
    的就是它，也就是说「手机上能渲染成卡片的链接」和「我们读得出标题的链接」基本是同
    一批。实测网易云、YouTube、GitHub、维基四个毫不相干的站全都给得出可用元数据。
  - 三层：**短链跟随**（一次 302，一段代码覆盖所有短链服务）→ **og 抓取**（绝大多数到
    这儿就解完了）→ **少数站的专门处理**（目前只有 B 站一家，它网页被风控挡着，要走自
    己的 API）。
  - **不做内容分类**：不判断这是音乐、视频还是新闻，只把标题原样交给模型。从「Rick
    Astley - Never Gonna Give You Up」它自己就看得出是首歌，比我们替它归类可靠 ——
    而且猜错了它会顺着错的往下编。
  - 抓不到就把链接原样留着，也就是**退回这个功能存在之前的行为**。需要登录的、被风控
    挡的、压根没填 og 的老站都会走到这儿。最多等 6 秒，比联网搜索那 12 秒短得多 ——
    搜索是模型在等结果，这个只是顺手，解不开照样能回消息。
  - 代理设置里多了「**链接预览**」一类（默认不勾）：国内站直连更快，YouTube / Instagram
    这类要走代理才读得到标题。

- **生图能选比例了**。以前一直出正方形，因为请求里**一个尺寸字段都不传** —— 那是故意
  的，各家默认尺寸不一样，写死一个很容易撞上「这个模型不支持 1024x1024」然后整个请求
  400。现在在生图模型的提示词下面加了一个比例选择，**默认那一档仍然什么都不传**，
  所以老配置行为一个字不变；只有你明确选了才发。
  - 选了的话 `size` 和 `aspect_ratio` **两个一起发**：OpenAI 那套认前者（`1024x576`
    这种像素串），Imagen 那套认后者（`16:9` 这种比例串），没有一个字段是通用的。多发
    一个不认识的字段中转站会忽略 —— 和 `negative_prompt` 同一个赌法。
  - 五档：1:1 / 3:4 / 4:3 / 9:16 / 16:9。像素值都取 1024 一边、另一边凑到 64 的整数倍，
    因为各家对宽高的约束基本都是「能被 64 整除」，凑不齐的值有些模型会直接拒。

- **本地 Mac 模式能在 macOS 26 以下跑起来了**（`scripts/patch-imessage-kit-schema.mjs`）。
  `@photon-ai/imessage-kit` 的数据库层只有一套 macOS 26 的查询，构造函数里写死，没有
  schema 探测也没有降级分支。它 SELECT 的列里有一批是 macOS 26 才加的，在 Sequoia、
  Sonoma 上那些列不存在，报 `no such column: message.ck_chat_id`。
  - 这个错**位置很靠后** —— 依赖装好了、chat.db 也打开了，要等真去读消息才炸，所以
    控制台一路正常，只有角色上线时变「未上线」，很难往这个方向想。
  - 补丁把这些新列换成 `NULL AS <原别名>`。安全是因为 mapper 那边读它们全走宽容的
    helper（遇 null 返回 false / null），严格的那几个只管 id、guid、date、service 这些
    老列，一个都不在替换名单里。代价是这些列对应的功能在低版本上本来就没有。
  - 幂等，认得出补过就跳过，原文件留备份。**重装依赖会冲掉补丁**，重跑一次即可。

---

## 1.1.1  2026-09-19

- **图发过来却像没看见，修好了**。角色偶尔会答得像对方什么都没发 —— 根子不在识图，
  在那张图**压根没送到模型面前**。`content.read()` 看着像本地取数，其实要往 Photon
  发一条 gRPC 流把字节拉回来，那条流会断；原来断了只记一行日志，这张图就当没收到，
  于是那一轮发给模型的是「1 条文本 / 0 张图」。
  - 实机日志里同一刻断的**不只是附件**：消息订阅流、投票订阅流、附件下载流三条一起
    挂。所以这不是「这张图有问题」，是底下那条连接抖了一下、挂在上面的流全断，那张图
    只是恰好在那一秒正在传 —— 换任何一张图、任何格式，结果都一样。
  - 现在断了自己重试，0.8s → 2s → 5s → 10s，最多读五次。等这么久是照日志里
    `attempt 3` 定的：SDK 自己是 500ms 起跳往上翻的，翻到第三跳说明这类中断不都是
    一两秒就好，只等两三秒照样丢图。而晚回几乎不算代价 —— 本来就有按字数算的打字
    停顿，一条图文消息隔十几秒才回反而更像人。17.8 秒之后就不再等，那已经不是抖动，
    该由桥接层的重连去管。
  - **不看错误自带的 `retryable`**：实机那条 `ConnectionError` 是
    `retryable: false` 配 `grpcCode: 14`（UNAVAILABLE，gRPC 语义里恰恰是「该重试」），
    两个字段反着的。所以按错误类型和文案认，认不出来的当不可重试 —— 附件不存在、
    超大小上限这些重试一万次也一样，只会白等。
  - **五次都读不到时，告诉模型这里本来有张图**：往那一轮里塞一句「对方发来一张图片，
    但没能加载出来，你看不到内容，别猜图里是什么」。图读不到是没办法，但「没看见」
    和「看见了看不清」是两种回法 —— 后者角色会说「图没加载出来，你再发一次」，
    而不是无视你发的东西。
  - 语音和文件走的是同一个 `read()`，一并修好。

- **线下模式：自己发的话马上就能看见**。以前发出去那句就消失了，要等角色回完才重新
  出现 —— 中间几十秒屏幕上没有你刚打的字。现在立刻画一条你那侧的气泡，正式存档一到
  就顶掉它，两件事发生在同一帧，不会闪重影。
  - 连接断在半路那种情况也兜住了：判据换成「后端存档里末尾是不是你这句」，而不是
    「有没有报错」。不然会出现存档里一句、输入框里还留着一句。

- **生成的时候能往上滑了**。以前每来一个字都把画面按回底部，手指划上去一下就被弹
  回来，看着像卡死。根子是 `scrollIntoView({ block: "nearest" })` —— 以为「翻上去之后
  目标不在视口里、浏览器就不会动」，但 `nearest` 的语义是**把目标滚进视口**，目标在
  下方视口外时它照样滚。
  - 现在两道闸，任一条成立就不动滚动位置：离底超过 120px（你在看旧内容），或者手在
    700ms 内碰过（滑动 / 按住 / 滚轮）。第二条是必须的 —— 只按距离判的话，划了 80px
    还没够阈值就被下一个字按回去了。700ms 是为了盖住惯性滑动。
  - 「手碰过」只认 touch 和 wheel，**不认 scroll** —— scroll 分不出人和程序，钉底
    自己那一下也会发一个，拿它记时间戳会把自己永久锁在让开状态。

- **回复失败或者你按了停，不用把那句重打一遍**。以前只有角色成功回复过的那条能重
  roll；生成失败、被你中断之后，得把自己刚打的话重新敲一次才能再发。其实你那句**早就
  落盘了**（后端是先存你这句、再去打模型），所以现在直接给一个「再来一次」，按同一份
  上文重新生成。
  - 顺带修掉一个原来就有的错：重 roll 和四条选项挂在「最后一条**助手**消息」上，
    而生成失败后末尾是你那句、最后一条助手消息成了上一轮 —— 那一轮的选项早被你的
    下一句接掉了，重 roll 也不是在重 roll 它。

- **手机上点输入框不再把整页放大**。iOS 的规则是聚焦字号小于 16px 的输入框就自动放大
  整页，**而且退出时不缩回去** —— 于是点一下输入框，三栏布局就永远歪在放大状态里。
  - 改字号，不往 viewport 里塞 `maximum-scale=1`：那等于把双指缩放一起禁掉，
    是条无障碍倒退。只在触屏下生效，桌面端 14px 的紧凑表单是设计的一部分。

- **日记的「注入近 N 天记忆」独立成一个旋钮**。以前这一路直接借记忆链那份，于是
  「聊天时回看几天」和「写日记时回看几天」被同一个数字绑死 —— 前者调小是为了省
  token，后者调大是为了让日记写得全，两个诉求反着来，调了一头另一头就歪。
  - 默认 3 天，和记忆链那份的默认一致，所以老配置升上来行为一个字都不变。

---

## 1.1.0  2026-09-19

- **报错终于说清楚是谁的问题了**。以前对方收到的是「⚠️ 回复失败：主 API（xxx）返回
  401」这种，一串数字加一个 API 名字，读的人只知道「坏了」。现在分两层写：数字翻成
  人话（401「密钥不对或已失效」、429「打得太频繁，或者额度/余额用完了」、503「上游
  这会儿没有可用的渠道」…），后面照旧附上游自己回的那句原文 —— 上游的措辞往往比
  我们的猜测准。
  - 然后按**归属**给一句「该找谁」，四档：上游明着回了错误码，就说「这是模型服务商
    回的错，不是 Uranus 的问题，请把上面这句话发给你的 API 服务商」；请求压根没到
    上游（代理坏了、域名填错），让先查网络和接口地址；内容审核拦下的，让换说法或换
    模型；**而「没填密钥」「没填模型名」这类是 Uranus 自己的配置没填全，明说是配置
    问题，不往服务商那儿推** —— 拿着「没填密钥」去问客服，两边都浪费时间。
  - 这句话接在 `notifyFailure` 这个唯一出口上，所以聊天、识图、语音转写、协助模式
    四条路一起生效，不用各自再写一遍。

- **主 API 出错时不再提副 API**。以前主 API 失败、副线没配，用户收到的话里带着
  「副 API 未配置」—— 于是一堆人以为是自己没接副 API，跑去配第二条线、换模型，
  折腾半天，而真正坏的是主线那把密钥。现在那句解释降级成**只写日志**（日志只有机主
  看，机主确实需要知道为什么没换线），用户那边一个字都看不到。
  - 只有**主副两条都真的失败过**时才提一句「副 API 也试过了，同样没通」—— 这时候
    提它不会误导人，对方配的两条线是真的都不通。

- **空回也写明白**。模型回了 200、正文是空的，以前只有一句「模型没有回复内容」，
  看着像 Uranus 把消息吃了。现在说清三件事：**这一轮的 token 照样会扣**、这是上游
  返回的结果、与 Uranus 无关，常见于推理型模型把输出长度全用在思维链上，或者中转站
  回了空响应。识图返回空描述、语音转写返回空结果同样处理。
  - 反过来，**正文是被正则吃掉的**那种明确说「这一条不是上游的问题」，指向
    「预设 → 正则」—— 这一类去问服务商是白问。
  - 控制台那条从 `warn` 提到 `error`，标题直接写「这一轮没有可发送的正文（token 已
    消耗）」。花了钱没出东西，该是红的。

- **「连上了但对方收不到」现在看得见了**。这是最难查的一类故障：token 扣了、模型回
  了、控制台一行绿的「回复发完」，对面一个字没收到。根子在 `space.send()` ——
  它在**消息进了 Apple 的队列**那一刻就返回，不等投递回执，所以整条链路上所有的成功
  判断其实都只到「我们交出去了」为止。号码压根没注册 iMessage、或者界面上号码填错
  一位，send() 照样成功。
  - 发出去 15 秒后去底层 gRPC 查这条消息的投递状态（`isDelivered` /
    `sendErrorCode`）。Apple 明着给了错误码就直接报；等够了还没回执，就再探一次对方
    这个地址，按结果分三种说法：**号码是通的但只支持短信/RCS**（对方用安卓、或者把
    iMessage 关了，这条线路发不了普通短信）、**查不到任何传输方式**（号码很可能填
    错了，去核对国家码和位数）、**iMessage 正常但没回执**（多半对方手机离线，上线后
    Apple 会补投）。
  - 错误码**不翻译**：Apple 没公开那张表，硬编一份猜的映射比原样给出码更容易把人
    带偏。给了码机主能拿去搜。
  - 连上的时候还会探一次**自己这条线路的号码**有没有注册 iMessage。这是「连上了但
    收不到消息」里最隐蔽的一种 —— 号码分配了、gRPC 连得上、控制台写着「已连接，等
    消息中」，但这个号码没注册成 iMessage，于是对方从 iPhone 发过去的是**绿色的普通
    短信**，走运营商网络、压根不进这条线路，两边都以为自己没问题。报警时会顺带告诉
    机主去问对方「你那边气泡是蓝的还是绿的」—— 绿色就是这个问题。
  - 同一个地址、同一种问题 10 分钟内不重复报，一轮回复拆成七条气泡也只报一次。
    全程 best-effort：这套检测自己挂了只记 debug，绝不影响聊天 —— 它是个诊断工具，
    不是链路的一环。
  - 只在明确探出问题时才报。探不出来（网络、权限、接口变了）一律不吭声 —— 连接刚
    起来就报一条「可能有问题」，只会让人以为连接没成。

- **电脑端侧边栏能展开了**。左边那条 60px 的轨道只有十三个图标，得靠 hover 出
  tooltip 才知道哪个是哪个。现在顶上多一个展开键，点开变成 200px 带分区名；旁边一个
  图钉可以让它一直摊着，**刷新后还在**（记在浏览器本地，不进配置文件 —— 它是「这台
  电脑上怎么看」，不该跟着备份和云同步跑）。
  - **刻意没做 hover 展开**：鼠标路过侧栏时整个布局跟着抽动，比费劲更烦。
  - 只过渡宽度、150ms，不刷底色不加投影 —— 和全站那套「只靠 1px 线分层」一致。

- **手机上保存键不用双指缩放才找得到了**。根子是 `100vh`：手机浏览器的 `100vh`
  算的是「地址栏收起后」的高度，地址栏和底部工具条是盖在上面的。而这套三栏布局是
  `h-screen` + `overflow-hidden`（整页不滚，只有列表和主内容各自滚），于是贴底的
  保存条正好落在地址栏底下，**而整页又滚不过去** —— 只能把页面缩小才看得见「保存」。
  - 换成 `100dvh`（当前实际可见的那一屏，随地址栏收放变化），老浏览器回落 `100vh`。
    登录页同样处理 —— 那儿是垂直居中，偏下去的话密码框和按钮一起被压住。
  - 顺带修掉一个撞车：右下角那个客服气泡是 fixed 的 48px 圆钮、压在保存条之上，
    375px 的屏上正好盖住「保存」的右半边，点不着。手机上给保存条留出右边 80px。
  - iPhone 上再让出底部那条小黑条（安全区），不然按钮看着像被屏幕边缘啃了一口。

---

## 1.0.9  2026-09-19

- **线下模式能边生成边看了**。一轮线下生成动辄几百上千字，以前只能对着转圈干等
  几十秒。「连接」面板上多一张「输出方式」的卡，三档：`auto`（默认）按流式发，
  上游回的不是 SSE 就当整段收下 —— 假流式的反代、压根不支持的中转，这一档下
  照样能用；`on` 强制流式；`off` 走原来那条非流式的路。
  - 这张卡放在「连接」面板、**全局一份**，和 TTS 一个道理：能不能流取决于你到
    上游那一段管子（中转站、自建反代、公司网关），换个角色不会变。
  - **只管线下模式**。iMessage 那条路要等模型把整段写完才知道拆成几条气泡、每条
    隔多久发，中间那半截没地方放。
  - 流式只改「什么时候看到字」：存档、正则、摘选项、记忆库一个字都不变 —— SSE 的
    `done` 事件带的响应体和非流式那条路**完全一样**，收完都是同一个 handler。
    只有「怎么收」的区别，没有「收到什么」的区别。
  - 前端没用 `EventSource`：那东西只能发 GET，而线下一轮要把几十 KB 的上下文 POST
    上去，所以自己读 `res.body`。401 的处理和 `api()` 对齐 —— 流式接口一样会撞上
    会话过期，那时候要把界面切回登录页，而不是在线下面板里干等一条永远不来的
    `done`。
  - 上游拒绝 `stream` 这个参数时脱掉它重发一次。这条降级不能光改参数：**读法**
    也得一起换回非流式，两件事得在同一个地方决定。

- **总结被内容安全拦下来时，换个说法再问一遍**。两种真实的失败：上游明着拦
  （`Prohibited Use policy`），以前重试 0 次直接失败；以及模型答应了、但回的是
  一句「很抱歉，我无法协助处理这个请求」。第二种更要命 —— 那句道歉以前会被当成
  一份合格的总结，而备忘录是**整份覆盖**的，一句道歉就能把攒了几个月的清单冲干净。
  - 换的说法是把「中立的第三人称摘要」这件事在提示词里说死。总结本来就该是那样，
    写明白了大多数时候就过得去。
  - 用尽之后**算失败**：pending 不清、备忘录不动。宁可这一轮没总结，也不能让一句
    道歉顶掉旧的那份。
  - 状态码卡在特定那几个上，别把 503「分组下无可用渠道」这类误判成内容拦截。

- **天气查不通就退回上一次存下的那份**。以前查失败直接 `return null`，这一轮彻底
  没有天气 —— 哪怕缓存里正躺着一份两小时前的。而代理坏掉和重启进程恰恰经常一起
  发生。
  - 上限定在 **24 小时**：再往前就是拿昨天的天气冒充今天，那还不如没有。
  - **不挑数据源**：和风查不通的时候，上一次退回 Open-Meteo 存下的那份照样能用。
  - 另外记一笔「刚才这个源没打通」—— 代理坏掉的时候三个数据源一个都打不通，没必要
    一家一家再试一遍。
  - 清缓存时盘上那份一起清。只清内存的话，重启一次刚清掉的天气又回来了。

- **带思维链的模型判断「等多久」，别再被截断**。这里的 token 上限曾经是 16，理由是
  「只要一个数字，16 个绰绰有余」。可带思维链的模型会把这 16 个 token 全用在思考上，
  回一段拦腰截断的思考、`finish_reason` 是 `length`，于是 `llm.js` 抛「返回格式
  看不懂」。
  - 现在剥掉 `<think>` 和代码块再找数字 —— 思考过程里的数字（「上次是 3 小时前」）
    不能当答案。一个数字都找不到（「明天早上」这种）才退到 `FALLBACK_HOURS`，
    退比不发好。
  - 截断和空回复两种都值得**不设上限再问一次**：一次判断而已，不差这点钱。

- **正则渲染出来的东西铺开显示，不再套在会滚的小框里**。两个毛病凑在一起，看着就是
  「网页里嵌了个网页」：iframe 自己带一道 `border` 和 `bg-paper`，夹在 widget 的
  圆角玻璃卡片外面；高度又被夹在 1600px，而状态栏、喵喵选择器展开后有好几千像素，
  于是**天天在滚**。widget 自带的圆角、玻璃底、留白就是它该有的样子，外面不该再包
  一层。现在边框和底色去掉，高度按框子自己上报的值直接铺开。
  - 「放高一点 / 收起这块」那个按钮连状态一起删了 —— 不裁了就不需要展开。
  - `MAX_H` 留着，但从版式上限变成**纯保险丝**：万一撞上「body 高度跟着 viewport
    走」的写法，两边能互相顶着往上长。`SIZE_SCRIPT` 那边有 40 次上报封顶兜着，
    这里再加一道，正常内容永远碰不到。

- **世界书的关键词框打得进逗号了**。以前「只能输一个词，逗号根本打不进去」：框是
  受控的、每敲一下就 `split` 回数组再 `join` 回来，而敲下逗号那一刻切出来的末尾是
  个空串、被 `filter(Boolean)` 扔掉，于是那个逗号**当场从框里消失**。现在正在编辑
  时以本地这份原文为准，数组只管往外存；失焦（或按回车）时用规范化后的结果把框里
  的字顶掉一次 —— 多余的空格、重复的词、连着打的两个逗号在**敲完之后**收拾，而不是
  在打字中途跳着改。

- 更新检查那条测试里，「远端有新版」那个 release 的版本号钉死在 `v0.9.0`，本地版本
  涨到 1.0.8 之后它变成「远端比本地旧」，那条断言**无声失效**了。改成 `9xx.0.0`，
  以后再涨也追不上。

---

## 1.0.8  2026-09-18

- **新增「强制分隔」**（发送节奏里的开关，默认关）。有些角色就是不吐 `$`，改用
  逗号句号、或者拿 `\n\n\n\n` 当段间隔 —— 于是整段话挤成一条巨型气泡，或者一堆
  空行拼出一条全是空白的「气泡」。这不是 bug，是提示词没约束住模型的输出习惯，
  而提示词是你自己写的、模型也不总照做。所以这一刀切在**输出**侧：开着的时候
  一律按逗号、句号、问号、叹号（中英两套）、分号冒号顿号、省略号、**任意个**
  换行、**任意个**空格切。「任意个」靠正则的 `+` 保证 —— 连着四个换行只切一刀，
  不会切出三条空气泡。和 `separator` 是**互斥**的两条路，不是叠加。
  - 切成的是「短句」不是「段落」，这是刻意的：你要的就是一条条蹦出来的观感，
    按段落切最后还是大段文字堆在一起。代价是英文句点一并切，`3.14` 会断成两条 ——
    「强制」二字的代价，不想被切就别开。
  - **方括号里的内容一个字都不切**。`[audio_message:我刚刚到家了，累死了]` 是
    一整条要发出去的语音，里面那个逗号是台词。硬切的话它会碎成
    `我刚下班[audio_message` / `我刚刚到家了` / `累死了]看这个` —— 语音发不出去，
    半个标记还当纯文字发给了对方。图片、卡片、点歌、引用全一个道理。所以切之前
    先把 `[…]` / `［…］` / `【…】` 整段挖走，切完原样填回去（认这三种括号是因为
    模型中英输入法混着用，`media.js:MEDIA_TAG` 认的正是这三种）。只吃一层，
    嵌套的交给下游 MEDIA_TAG —— 这里的任务是「别把它切开」，不是替它解析。
  - **存进历史的也是切好的那一份**，按 `separator` 拼回去。不这么做的话，发出去
    是七条气泡、历史里却躺着一整段「你好呀，我刚刚订到…」的话，模型下一轮照着
    上文学，只会更不肯自己分段 —— 「开着好好的、一关掉立刻退回大段文字」就是
    这么来的。改写和发送**共用同一个函数**（`delay.js:forceSegments`），不是两套
    各切一遍，所以「发七条记六条」这种不一致从结构上就不可能发生。注意这一步
    **不是无损的**：`3.14` 在记录里也是 `3$14`，和实际发出去的那两条对得上 ——
    历史要的是「和发生过的事一致」，不是「还原模型原文」。IG 回合里只改写那条
    短信，`commentLine` 不动（它是我们写给模型看的旁白，压根没走 `sendBubbles`）。

- **拖着条目的时候滚轮能用了**。一百多条的预设里把一条从末尾拖到开头，以前只能
  「拖到屏幕边上干等 → 松手 → 滚轮 → 再按住」重复好几轮。曾经挂过一个全局补丁
  想在拖动期间接管 `wheel` 自己滚，但那个补丁的前提是错的：原生 HTML5 拖放一
  开始，Chromium 就进了拖拽控制器自己的事件循环，`wheel` **根本不派发给页面**
  （只剩贴到视口边缘的自动滚动）—— 监听装得再准也收不到，从写出来那天起就没
  生效过。现在整套换成 `pointerdown` + `setPointerCapture`：拖动只是我们自己在
  记坐标，浏览器没进任何特殊模式，于是滚轮是一个普通事件，不需要任何补丁。
  - 落点按**中线**判定，不按「进入元素范围」：行高不一（条目展开着编辑器时有
    几百像素），按范围算的话拖过一条高行时落点会在它的头尾之间反复跳。
  - 整个拖动过程一次 `setState` 都不做，落点提示直接改 className。走 state 的话
    每次 `pointermove` 都是一次全列表重渲染，一百多条、其中还有展开着 CodeArea
    的，拖起来直接卡成幻灯片。
  - 触屏照旧交给 ↑/↓ 按钮。`touch-none` 只加在握把上 —— 不关掉它，手指按住握把
    会被当成「要滚页面」，`pointermove` 直接变 `pointercancel`；只加在握把上，
    列表其余地方仍然能用手指滑。

- **桌面端有细滚动条了**。以前是全局 `display: none`：手机上没问题（手指直接拖
  内容），桌面上就是灾难 —— 一百多条的列表滚轮甩半天不知道自己在第几屏，也抓不
  住那根槽往回拖。现在 8px 槽 + 圆角滑块，滑块用 `--ink` 的 32% 透明度（换算到
  纯白上是 `#c6c6c8`，比 `--line` 深一档），悬停 48%、按住 62%。起点试过 22%
  （`#d1d1d1`）比边框线还浅，缩略图里基本看不见，等于没修。槽留透明：只在滑块
  和纸张之间做明度差，不额外画一条 8px 的边，否则整页右侧会多出一道灰边。
  - 触屏仍然全藏，判据是 `pointer: coarse` 而不是宽度断点 —— 一台 1280 宽的平板
    该藏，一台 800 宽的鼠标笔记本该显示，决定的从来不是屏有多宽。
  - 挂在 `[data-scroll]` 上而不是 `*`：`*` 上的 `scrollbar-width` 会连带改掉
    textarea 之类，谁要滚动条谁自己声明。

- **线下界面划到顶不再是一片空白**。这一页只画最近 20 轮（一条 HTML 回复就是一个
  iframe，几百轮一起挂着页面会明显发涩），更早的那批本来靠顶上一个按钮一次性
  展开 —— 可那批轮次根本不在 DOM 里，按钮又只有豆大一个，一下划过去就以为
  「聊天记录没了」。现在改成往上滚就自动补，一次 20 轮，滚到哪看到哪。**旧记录
  一条都没丢**，只是没画出来。这个行为从 1.0.1 就在，不是最近改出来的。
  - 补进去的轮次插在列表开头，浏览器会把下面的内容整体往下推，用户正盯着的那条
    会突然跳走。所以动手之前先量「那一条离容器顶多远」，补完在 `useLayoutEffect`
    里把这个**距离**还原（绘制之前改，看不见跳动）。还原的是距离而不是「推回
    容器顶」—— 后者会让画面整段跳掉那段距离（实测 552px）。
  - 用 `scroll` 而不是 `IntersectionObserver`：IO 在后台标签页里根本不回调。
  - 顶上那行提示留着（「上面还有 N 轮」），一是划到顶看见它比看见空白强得多，
    二是它还能手点。补到底时 `skipped` 归零，这套自动停摆。

- **线下的渲染框子不再「有框没字」**。沙箱框子没有 `allow-same-origin`，于是
  **光是读 `window.localStorage` 这个属性**就当场抛 `SecurityError`（不是
  `getItem` 抛，是取属性就抛）。而酒馆那边的 widget 拿 localStorage 当主题总线是
  常规写法，开头一句 `localStorage.getItem(…)` 没人接 —— 一抛，整段 `<script>`
  就地终止，**后面渲染选项的代码再也跑不到**：外壳是静态 HTML 所以照样画出来，
  里面一个选项都没有。现在在 widget 自己的脚本**之前**用 `Object.defineProperty`
  垫一份内存实现（`<head>` 后第一个位置，插晚了就白垫）。不用 try/catch 兜是因为
  兜不了 —— 那是别人写的 widget，我们不改它一个字节，改了下次导入又是原样。
  垫的这份活不过这个框子，每次重挂就是一份新的空存储；真让它写进本页面的
  localStorage 反倒是污染，几十个框子抢同一个键。**`allow-same-origin` 一个字
  都没动** —— 那是安全边界，控制台页面里有 API key 和 Photon 凭据。

---

## 1.0.5  2026-09-18

- **手机上能用了**。以前 375px 的屏上四样东西一起挤：常驻的 60px 导航轨、
  已经收着但没入口的 260px 条目栏、左右各 24px 的内容区留白、以及被压成
  **0 宽竖排**的标题 —— flex 里标题带 `min-w-0`、右边那排按钮带 `shrink-0`，
  宽度不够时全让给按钮，「角色」两个字就竖着码起来了。只收侧边栏治不了：
  那栏本来就是 `hidden md:block`。所以四样一起改，全在 `md:` 断点以下，
  **桌面端的类名逐字没动**（量过：导航轨 60px、条目栏 260px、留白 40px、
  手机顶栏 `display:none`）。
  - 导航轨和条目栏都收进覆盖层，从新的手机顶栏两个按钮点开（左汉堡出分区、
    右列表出当前分区的条目），中间是当前条目名。跳分区、选条目、按 ESC、
    点遮罩都会关掉。
  - 手机上那份分区列表**没有复用桌面那条轨**：手机没有 hover，十三个纯图标
    方块认不出是什么，必须带文字；而桌面那条轨的硬规则是「仅 20px 纯色图标」。
    两种形态差的不是几个类名，是要不要有文字。
  - 内容区留白 24px → 16px（`px-4 md:px-6 lg:px-10`），375px 的屏上 24×2
    是实打实的一行字。标题那行在 `sm:` 以下改成竖着堆，标题占满一行、按钮
    另起一行，谁也不用抢宽度。

---

## 1.0.4  2026-09-18

- **线下界面不再露出几种控制标记**。上一版改成存模型原文之后带出来的：
  `<snow>`、`<Drama>`、`[[伏笔]]…[[/]]` 这几种标记在「神本无相」那类预设里
  **只有**「不对 AI 发送多余内容」那条 `toHistory` 删除规则认得，`toUser` 那
  一路没有任何规则接手。以前落盘存的就是过完 `toHistory` 的文本，所以这些标记
  在界面上也跟着消失了 —— 靠的是副作用，不是有人真的决定要这么显示。现在存
  原文，它们就原样糊在气泡里。收的是一张**短黑名单**（`offlinehtml.js:dropStray`），
  不是「白名单之外全收」—— 后者试过，代价太大：`<Chronicle>` 里面是小说题头的
  正文，换一份没开那条渲染规则的预设就会被整段吃掉；而 `<html>` 不在
  `HTML_TAG` 那张表里，按它反推还会把一整份五万字的状态栏 widget 删掉。所以
  只收「里面的东西本身就不是给人读的」那几种，拿不准的一律留着 —— 界面上多
  一行标记，比用户的正文凭空少一段轻得多。没人渲染的 `<状态面板>` 之类照旧
  原样显示，那正是「该去开哪条规则」的线索。

---

## 1.0.3  2026-09-18

- **状态栏不再「只能看一次」**。用正则渲染状态栏的预设（「神本无相」那一类），
  发出去那一刻状态栏是好的，页面一刷新就变成空白 —— 翻旧剧情永远看不到。根因
  在存档：线下这条链以前落盘存的是**过完 `toHistory` 的那份**，而这类预设里
  往往有一条「不对 AI 发送多余内容」的删除规则，会把 `<状态面板>` 整段从存进
  剧情的正文里剥掉。可界面上那份（`display`）是每次打开剧情时拿 `content`
  现算的，原料没了，`toUser` 那条渲染规则再宽容也画不出东西。现在改成和
  iMessage 那条链同一个做法：**存档存模型原文**，`toHistory` 那一路挪到
  `prompt.js:filterHistory`，拼提示词时才跑。模型看到的上文照旧干净（那堆
  `<div>` 绝对不能进上下文，否则它下一轮就开始模仿自己吐 HTML），而
  「改了渲染正则整条剧情立刻跟着变」这条本来写在文件头的取舍也才真正成立。
  总结那一路跟着补了一道：`turnsText` 里助手侧先过一遍 `toHistory` 再喂进去，
  不然几千字的面板会连着进总结材料，白烧 token。**已有的旧轮次救不回来** ——
  那些 `content` 里的面板在落盘时就没了，这次改的是以后每一条都不再丢。
- **线下渲染块的高度上限从 640 提到 1600**。一条回复里状态栏和剧情选项是两个
  独立的渲染块，各自量各自的高度。状态栏展开态就有近 700px，640 的上限会把
  选项那块整个裁到可视区外 —— 看起来像正则没生效，实际上它渲染得好好的。
- **界面上「导出」按钮产出的单件文件不再进版本库**。预设正文是别人写的（有的
  作者明确不许二传），世界书和记忆库的导出还会带上聊天里的私人内容。

---

## 1.0.2  2026-09-17

- **线下剧情里的气泡按字数量宽了**。以前每条都撑满整行，一句「好」也横着穿过
  去、右边停在对面头像底下 —— 谁说的那句话只能靠头像认，认不出形状。现在不带
  widget 的那条走 `w-fit`，自己那侧再收一道 34rem：你打的字是「说了一句话」，
  不该和角色那边整段正文一样宽。带 widget 的**仍旧撑满**，这是有意的 —— 里面
  那些 iframe 是 `w-full`，宽度反过来靠外面这层给，外面一收缩它就塌回 300px
  （替换元素的默认宽），整块 widget 缩成窄条，比原来还难看。

---

## 1.0.1  2026-09-17

- **线下模式生成到一半能按停了**。以前发完一句就只能干等，模型跑偏了也得看它
  把几千字写完。现在按的是**真中止**：路由按角色名登记这轮的 `AbortController`
  （`index.js:offlineAborts`），`/abort` 把它掐掉，信号一路传到 `llm.js` 里和
  超时那个信号 `AbortSignal.any` 并起来，断的是上游那个 fetch，不是前端假装
  放弃、后台接着烧 token。两个坑一并处理了：被 abort 掐断的 fetch 抛的是
  `AbortError`，而重试逻辑把 `AbortError` 当「网络抖动」—— 不先拦一下，按一次
  停会换来三轮「800ms 后重试」再去打一遍副 API；同理 `chatWithFallback` 认
  `err.aborted` **不换线**，按停了就是按停了，不是主 API 坏了。**你那句话早就
  落盘了**，所以错误条旁边直接挂一个「再来一次」，按同一份上文重来，不用重打。
- **用正则渲染状态栏的预设（「神本无相」那一类）不再糊成一坨**。两层根因，
  各修各的。一层在服务端：替换词里的 `$` 以前直接交给 JS 原生的
  `String.replace`，而它把 `` $` `` 认成「匹配点之前的全部正文」、`$'` 认成
  「之后的全部正文」—— 替换值里只要有一个这样的字符，整段回复就被复制好几份
  贴回去，看着就是满屏重复。现在自己展开（`regex.js:expandMatch`，前端试跑那
  份是镜像），只认 `$$` / `$&` / `$1`..`$99` / `$<name>`，那两个魔法不认，其余
  带 `$` 的写法原样留着。另一层在前端：一条回复里常常是「题头片段 + 一整份
  `<!doctype html>` 的 widget + 散文 + 另一个 widget」，以前**整坨**塞进同一个
  iframe 的同一个 `<body>`，于是 widget 自带的 `body{display:flex}` 和字体作用
  到了所有东西，题头和散文被挤成并排细条、散文还选不中也复制不了。现在先按
  文档边界切开（新的 `client/src/offlinehtml.js`，纯字符串那半能在 Node 里直接
  验），每份完整文档各自一个框子、散文回到页面上用正常排版画。
- **「对话框」这个名字全站没有了，一律叫「线下模式」**。分区名、右上角的路径、
  角色页的跳转按钮、命令帮助、`/帮助` 的正文、README 的分区表、助手知识库里
  那几条「去哪儿改」和「为什么不生效」，一处不剩。顺手把顶上那块瘦了一圈：
  分区说明从三行长句砍成一句，那行「剧情 1 · 4 轮 · 预设「…」」的角标去掉 ——
  同一屏里剧情本身能多占一截，才是这个页面该有的样子。
- **`[Image:]` 大写开头也出图**。内置的「线上默认预设」自己写的就是
  `Output format: Must start with [Image:].`，模型照着写出来是大写的，而标签
  正则以前只认小写 —— 那句标记原样当短信发出去了，一张图也不出。预设是导出来
  互相分享的，让每个人回去改一遍不现实，所以改正则：`MEDIA_TAG` 和 IG 那边的
  `igtags.js` 一起加 `i`，两边必须同一个口径，否则同一条输出在私聊里能出图、
  在 IG 那条路上认不出来。同一条正则里的语音、表情包、卡片、点歌跟着一起不分
  大小写 —— 同一个毛病，没有理由只修图片这一个。
- **总结和提示词都改成弹窗，主界面只留剧情**。总结那一栏原来常驻在剧情下面占
  掉小半屏；提示词那份以前只有「上下文」分区看得到，可线下比线上更需要看它
  （预设更长、世界书更多），后端本来就把线下那条链记进同一个槽，只是没有入口。
  两个都收进弹窗，用的是共用的 `Modal`（只压暗、不模糊 —— 全屏 `backdrop-blur`
  会逼合成器把底下 `background-attachment: fixed` 的壁纸每帧重糊一遍）。
- **剧情默认只画最近 20 轮**，更早的那批点一下一次展开。卡顿的根源是每条带
  HTML 的回复都挂着一个 iframe，几十轮一起挂着整页都发涩 —— 滚动、打字、连
  切分区都跟着掉帧。
- **线下语音的播放键改成纯图标的小圆键**：空闲一个 ⊳、合成中转圈、正在念 ⊡、
  出错 ⊗。原来那个带字的按钮又宽又显眼，还会长时间停在「合成中」上不动。
- **世界书不再误报「还没有角色用它」**。那句提示以前只扫 `role.worldBookRefs`
  一路，可线下剧情走的是**另一份书单**（`role.offline.worldBookRefs`），日记
  在「不跟角色走」时还有第三份 —— 只挂在线下的书明明在生效，界面上却一直劝你
  「去挂上」。三路一起查，另外在生效的书底下把「在用它的：线上：… ；线下：…」
  摊开写：两边是独立书单，用户最需要看清的恰恰是自己勾的是哪一边。
- **预设条目支持 `{{lastUserMessage}}`**，值是上文里你说的最后一句。用法是在
  预设最末尾单开一条 `role: user` 的条目把它再说一遍，让模型回复的直接对象是
  你那句话，而不是中间几千字系统指令。取的是**过完正则之后**的那份 —— 被滤掉
  的部分模型本来就看不见，复述一遍等于捅回去；第一轮或主动消息那种你还没开口
  的场合它是空的，引用它的条目整条不产出。

---

## 0.9.4  2026-09-17

- **上游说哪个生成参数不收，就脱掉哪个重打，不再整轮作废**。一批用户报的
  `400 Unsupported value: 'temperature' does not support 0.7 with this model.
  Only the default (1) value is supported.` 就是这个 —— 0.7 正是预设里
  `DEFAULT_PARAMS` 的默认温度。原来那段注释写着「没配的就别发」，但
  `normalizeParams` 会把这几项填成默认数字、永远不为空，实际上是**无条件发**
  的；撞上把采样参数锁死在默认值的推理型模型（OpenAI 的 o / GPT-5 系那类）
  整轮 400，而 400 不属于可重试，副 API 拿着同一份参数再废一次。修法没走
  「再维护一张模型名黑名单」—— 中转站的模型名五花八门、新模型每周都有，
  维护不过来。改成认**上游的抱怨**：它指名道姓说哪个字段不行就脱掉哪个，
  立刻重打、不等、也不占网络抖动的重试额度，脱到能过为止。
- **报错不再把人往副 API 上引**。原来主 API 失败又没备胎时，抛出去的话尾巴上
  永远挂一句「（副 API 没启用或引用的模型已失效）」。它本意是「我们没线可换」，
  可跟在一句 401 / 503 后面读起来就是失败原因 —— 用户照着去折腾副 API、连换
  DeepSeek、月之暗面、Gemini 3.7/3.8，换一圈还是同样的报错，因为 **401 是密钥
  那一层的事、503 是中转站那一层的事，跟选哪个模型没关系**。现在明说两件事：
  前面那句是模型服务商回的，以及我们为什么没换线。
- **日记改成弹窗**。点日历上的一天直接弹出来，在弹窗里看、改、删，不再在日历
  下面展开一大块。用的是共用的 `Modal` —— 它**只压暗、不模糊**，这正是「不掉帧」
  的关键：全屏 `backdrop-blur` 会逼合成器在每敲一个字、每滚一行时，把底下
  `background-attachment: fixed` 的壁纸整幅重新模糊一遍。旧版「不做成弹窗」
  唯一站得住的理由是怕丢稿，在这儿消化掉了：有没写盘的改动时，ESC / 点遮罩 /
  右上角 × 都先拦下来问一句，确认了才真的关。
- **导入预设认「裸的预设文件」了**。`data/presets/` 里一份一个的那种存储文件，
  从备份里翻出来直接拖进导入是最顺手的动作，原来会被一句「这不是 Uranus
  iMessage 导出的文件」拦在门外 —— 那句话只认「导出」按钮产出的信封格式。
  判别用预设独有的字段（`mode` / `regex` / `params`）：世界书的存储文件也长着
  `entries`，光看那个会认错人。顺手认一下 SillyTavern 的预设（`prompts` +
  `prompt_order`），明说「两边格式不同，导不进来」比一句笼统的报错有用。
- **按住拖动的时候滚轮能用了**。原生拖放的老毛病：拖动期间 `wheel` 事件照发，
  但浏览器把「滚」这个默认动作吞了（Chromium 多年如此），拖一条长列表走远路
  只能拖到屏幕边上干等。现在只在拖动期间接管 `wheel`，从事件目标往上爬找最近
  一个真有得滚的容器手动滚它。预设条目、预设正则、世界书条目一起受益。
- **正则列表里的替换词不再撑出卡片**。从酒馆转过来的规则，替换词可能是一整段
  HTML（状态栏那种），角标里放不下就把整行顶到卡片外面去。角标截到 24 字
  （空白先压成一个空格），全文点编辑看；同时把那个角标从 `shrink-0` 改成
  `min-w-0 max-w-full truncate`，宽度是真的收得住。

---

## 0.9.1  2026-09-17

- **Windows 上多了个 `更新.bat`，双击就升级**。以前 ZIP 装的人更新要「重新下一份、
  解压到新文件夹、把 `data` 拷过去」，`node_modules` 还得重装一遍好几分钟。现在是
  **原地换文件**：停服务 → 备份 → 换代码 → 起回来，`node_modules` 从头到尾没动过。
- **依赖没变就不装**。判据是更新前后各给三个 `package.json` 的依赖块算一次
  SHA-256 再比（`depFingerprint`），不是 `启动.bat` 里那个手写的哨兵清单 ——
  那个清单要靠人记得往里补包，忘一次就是所有人一起炸。常见情况下这一步是零秒。
- **换文件之前整套备份**，进 `.update-backup\日期_时间\`，留最近三份。每份里有个
  `这是什么.txt`，写了备份时间、从哪个版本更到哪个版本、动了多少文件。新版起不来
  就把里面的东西按原样盖回根目录。
- **撞上你自己改过的文件会停下来报错，一个字节都不覆盖**。有 `.git` 的走
  `git pull --ff-only`，工作区脏就拒绝并把改动列出来；没 `.git` 的靠
  `.update-state.json` 里上一版每个文件的 SHA-256 认出「哪些是你动过的」，
  对不上就停。第一次用没有指纹可比，会**明说**查不出来，而不是假装查过了。
- **`data/`、`node_modules/`、`.sandbox/` 永远不碰**。前两个本来就不在源码包里，
  这里再拦一道（`PROTECTED`）—— 「更新把聊天记录弄没了」这种事发生一次就没法挽回。
- **从没有 `更新.bat` 的老版本也能上来**：`更新.bat` 发现 `scripts/update.mjs`
  不在就自己下一份。太老的版本连 `更新.bat` 都没有，在项目目录里跑一行
  `curl.exe -fsSL -o scripts\update.mjs https://raw.githubusercontent.com/nikonotnicotine/Uranus_Imessage/master/scripts/update.mjs; node scripts\update.mjs`
  就能补上，更完 `更新.bat` 就在文件夹里了。（`curl.exe` 不能简写成 `curl` ——
  PowerShell 里那是 `Invoke-WebRequest` 的别名，不认 `-fsSL -o`。）
- 几个只有踩过才知道的坑，一并处理了：解包用手写的 tar 解析器而不是系统的
  `tar.exe`（bsdtar 按当前代码页 936 解文件名，`启动.bat` 和 `docs/VPS部署教程.md`
  会变乱码）；下载 curl 优先、`fetch` 兜底（curl 认 `HTTPS_PROXY` 和 `.curlrc`，
  Node 的 `fetch` 两个都不认）；`更新.bat` 最后一行把 `node` 和 `exit /b` 写在
  **同一行**（cmd 是按字节偏移边跑边读 .bat 的，而这次更新会把这个文件本身换掉）。
- Mac 和 VPS 上跑 `scripts/update.mjs` 会直接告诉你用 `git pull && npm install &&
  npm run build` 那一行 —— 停服务靠 `taskkill`、重启靠 `cmd`，都是 Windows 专属，
  与其在那边跑出个半截结果，不如明说。

---

## 0.9.0  2026-09-17

- **八股文规则进了正则栏，能开关了**。原先线下预设那套「去八股文」是服务端
  硬编码的回退规则 —— 界面上看不见、动不了，用户原话是「我要在正则里的，不然
  我都不知道我要怎么开关」。现在 25 条（8 条删除 + 17 条替换）是**真正的正则
  规则**，和用户自己加的规则一视同仁：一条条开关、拖动排序、跟预设一起导出。
  默认全关，用得上哪条开哪条。内容本身没变（极其 / 一丝 / 指尖泛白 / 岩浆
  那批），就是搬了个家 —— 单一事实来源在 `server/src/cliche.js`，客户端和
  `assets/` 里的种子预设都从它生成，改一处三处一起变。
- **正则规则能单独导出、单独导入**。导出的是一个 `uranus-regexrules-<预设名>.json`
  文件，导入是**往眼前这份预设里补几条**，按 id 去重（同一份文件导两次不会
  翻倍，被删掉的几条倒是能补回来）。想把这套八股文规则贴到自己别的预设里，
  不用连整份预设一起搬。规则导入**保留原 id、不改名** —— 顺序有意义
  （`rx-r-16` 必须排在 `rx-r-17` 前面），重发 id 会把顺序和「谁是原来那条」
  一起抹掉。
- **定时清缓存 / 重启 / 云备份改成「每 X 天 X 小时」，而且计时不再被重启清掉**。
  间隔拆成「天 + 小时」两个框（填「每 1 天 6 小时」不会再被显示成「每 30 小时」，
  想填「每 3 天」也不用自己心算 72）。**进度落盘**（`data/maintenance.json`）——
  以前三个计时器是内存变量、进程起来时设成启动时刻，于是「每 3 天备份一次」在
  一个每天重启的服务上永远等不到点。现在从**上次真干过的时刻**接着数：重启服务、
  关掉再打开，倒计时都接着走；到点那一刻服务正好停着，回来就跑一次，中间那几次
  不补。开关关着的时候什么都不推 —— 关半天再打开不会白等一个周期。
- **修好「连不上」的报错只说 `fetch failed`**。真实原因有时埋得比 `e.cause`
  更深：Happy Eyeballs 在 IPv6 和 IPv4 都连不上时抛的是 `AggregateError`，
  `code` 挂在外层、里面每一条才带具体原因；代理隧道里出的 TLS / HTTP 错也会再
  包一层。现在会往下挖四层把 code 全捡出来，并直接说人话 —— 没走代理就提示
  「国内连不上，去控制台的代理那节勾上并填地址」。
- **老配置自动转换**。只填了总小时数的老预设（比如 168）在设置页里会显示成
  「7 天 0 小时」，保存一次就写成新格式。间隔上限 90 天；两个框都填 0 按 1 小时算
  （每跳都触发是灾难不是意图）。

---

## 0.8.0  2026-09-17

- **修好 Gemini 3.7 / 3.8 一发就 400**。这两个版本比别的模型多两条硬规矩（实测）：
  消息数组不能以 assistant 结尾，带生成参数（temperature / top_p / 两个惩罚项）也
  直接报错。现在按模型名认出来，走这两条规矩：末尾的预填正文挪到 user 名下发出
  （预填的效果在这两个模型上拿不到，但正文里写的格式要求模型还是看得到），
  生成参数一个都不发 —— 预设里配了也当没配，发了整轮请求就废了。
  预设里那条「卡思维链（预填）」在 3.1、2.5 和别家模型上照旧。
- **去掉「说什么语言」**（角色 → 上下文与语言 → 语言）。它会往提示词里填
  `{{language}}`（默认「中文」）和预设里那句「用正确的 {{language}} 标点符号」，
  实际效果只是让人设和语音各说各的语言。设置项删掉、变量删掉，预设里残留的
  `{{language}}` 不再替换、原样留着（看得见才好改）。
- **记忆库手加 / 手改能填日期和关键词**。日期不填就是今天，填了就是补记到那天
  （格式不对会拒绝，不会悄悄算成今天）；关键词和按正文自动抽的合并去重。
  保存时顺手把这条的向量当场算出来 —— 配了向量模型的话，加完立刻能进语义检索，
  不用再等一轮总结。算不成只是跳过（界面上标「未向量化」），不挡保存；
  只改日期 / 关键词时不会白打一次接口，旧向量还是对的。
- **「导入 / 导出」导完自动补算向量**，不用再手点一次。
- **记忆页的报错会说人话**：报错文本只在下次成功时才清，所以「那时候还没选模型」
  这类已经解决的原因会一直挂在界面上；现在检测到原因已经解除就换个说法。
- **带语气标签的语音不再被截断**。模型写 `[audio_message:[whispers] …正文… ]`
  时，解析在第一个 `]` 上就提前闭合，抠出来的语音只剩九个字。语音和图片这两组
  的标记体现在允许**嵌成对**的方括号；不成对的照旧在它前面停下（老行为）。
- **语音里的语气标签按 TTS 认不认来剥**。ElevenLabs 的 `eleven_v3` 认
  `[whispers]` 这类标签，留着；v2 和另外两家会把它们当正文念出来（语音里真的说
  一句 "whispers"），那就剥掉。整条剥完一件不剩时留原样，免得失败无声无息。
- **线下模式不再把媒体标记当普通文字发出去**：语音变它要念的那句话、卡片退成
  网址、位置退成地名，图片 / 表情包 / 撤回 / 回应直接丢掉，整条都是要丢的标记时
  这条就不发。线下预设里那条「线上聊天记录」会把带标记的线上发言摆到模型眼前，
  有样学样写出 `[audio_message:…]` 不是不可能。
- **主动消息「对方读了没」成了开关**（角色 → 主动消息，默认开）。之前是写死的
  行为；默认开是因为它不额外打模型、不多发消息，只是给已经要发的那条多缀一句话。
  真正生效还要「已读与不回 → 已读回执」开着。
- **新增三份部署教程**：[Windows](docs/Windows部署教程.md)、
  [Mac](docs/Mac部署教程.md)、[VPS](docs/VPS部署教程.md)。

## 0.7.0  2026-09-15

- **控制台登录**。默认账号密码都是 `Uranus`
- **快速配置卡片**：新用户第一次打开自动弹出。
- **内置默认预设**：`assets/presets/` 里放着线上和线下两份调好的预设，
- **Uranus 助手**（右下角那颗气泡）：内置一本功能手册和一张症状表。
- **出网代理**（控制台 · 代理）：填一个代理地址，九类出网各一个勾，想走代理的单独勾上。
- **控制台 · 检查更新**：点一下去仓库看有没有新版本，给出更新说明和下载链接。
- **协议换成作者自己的非商业许可**：原来挂的是 MIT，但 MIT 正文明确允许「sell copies」
  和「sublicense」，和作者「不许商用、不许接入商业平台」的意思直接打架 —— 挂着 MIT
  等于那几条附加声明是空话。现在 [LICENSE](LICENSE) 是作者自己写的一份**非商业使用
  许可**，禁止商用放在第零条，README 顶部也放了一份简版。代价是它不再是标准协议，
  别人一看名字不认识，可能不敢用在正经项目里；换来的是条款说了算。
- **删掉开发模式**（`开发模式.bat`、`npm run dev`、5173 那套 Vite 热更新链路）：
  只剩生产这一种形态 —— 8787 出控制台、6873 出 Instagram，都由后端 serve 同一份
  `client/dist`。改前端代码要 `npm run build`（双击 `启动.bat` 会自动构建过期产物）。
  删它的理由：端口行为不再随模式变，少一份要同步的文档，少一个「你在哪个模式里看到的」
  的排查分支。唯一的例外是 `scripts/dev-sandbox.mjs`，那是开发这个项目时用的假数据沙箱，
  不是给使用者的入口。

## 0.6.9 · 2026-09-11
- **Tavily 搜索优化**：新增「保留字段」（日期/标题/正文）及「最低相关度过滤」（默认 0.65）。
- **核对说明**：确认备忘录生成时已正确附带现有备忘录全文。

## 0.6.8 · 2026-09-10
- **新增「角色主动消息」**：
  - 支持「随机等待（1-3小时）」与「自主判断（由模型决定等待时长）」两种模式。
  - 支持免打扰时段（默认 00:00–08:00）及发送后冷却防刷。
  - 感知已读回执：若对方已读未回，下次主动消息会自动追加状态提示。
  - 纯内存运行与发信保护，不会污染聊天存档与历史记录。

## 0.6.7 · 2026-09-10
- **后台自定义壁纸**：支持本地上传背景图片并自由调节白色遮罩透明度（即改即生效，独立存储，不触发服务重启）。

## 0.6.6 · 2026-09-10
- **记忆与日记增强**：
  - 新增日记界面「自己写一篇」手工录入入口，支持日历按天补写。
  - 优化向量记忆批量导入机制（支持时间戳正序重构与断点防丢）。

## 0.6.5 · 2026-09-10
- **新增快捷指令**：
  - `/重roll`：撤回当轮回复并重新生成（不计入待总结记忆）。
  - `/重启`：快速重启整个后台。
- **控制台「服务控制」**：
  - 新增进程重启与一键清理缓存（清理地理编码、正则与临时会话，不碰持久数据）。
  - 日志新增 `CRITICAL` 级别，默认过滤为 `INFO`。

## 0.6.4 · 2026-09-10
- **稳定性与格式修复**：
  - 修复 Photon 桥接断网后无法自动重连的问题，加入阶梯式退避机制。
  - 修复地理编码网络超时卡顿 8 秒的问题，内置 69 个常用城市坐标并加入失败缓存。
  - 待总结与日记流水统一补齐中文时间戳，并剔除联网搜索内容以节省 Token。
  - 规范记忆注入格式，仅过往回忆保留提示词说明。

## 0.6.3 · 2026-09-09
- **修复中转 400 报错**：总结链路统一改用 `user` 身份封装，解决部分 Gemini 代理提示 `contents is not specified` 错误。
- **流程优化**：总结顺序调整为“先记忆、后备忘录”；修复手动触发失败占用自动退避额度的问题；控制台报错支持展开完整日志。

## 0.6.2 · 2026-09-09
- **修复记录污染**：环境感知前缀（时间、天气）不再写入待总结记忆与日记流水。

## 0.6.1 · 2026-09-09
- **记忆库交互重构**：
  - 记忆库界面划分为「记忆 / 备忘录 / 日记」三大入口与独立设置抽屉。
  - 记忆模块支持按日聚合、检索与批量编辑；日记支持日历可视化；备忘录支持整体在线修改。

## 0.6.0 · 2026-09-09
- **全新角色记忆系统**：
  - **记忆**：满 15 轮自动提炼 + 余弦向量检索召回。
  - **备忘录**：周期性覆盖式生成长期状态/代办清单。
  - **日记**：第一人称日记流水，支持定时或手动触发，失败绝不丢流水记录。
  - 配置新增 `embedding`（向量模型）分类。
  - 新增快捷指令：`/memory`（即刻总结记忆）、`/diary`（即刻生成日记）。

## 0.5.0 · 2026-09-09
- **富媒体输出支持**：
  - **语音条**：接入 MiniMax、ElevenLabs、GPT-SoVITS，发出原生 iMessage 语音消息（内置 ffmpeg 转码）。
  - **AI 生图**：支持文本生图与基于图库的“图生图”（快捷指令 `/image`）。
  - 新增「参考图」分区及语言变量 `{{language}}`。

## 0.4.4 · 2026-09-08
- **联网搜索调优**：
  - 搜索频次、结果数、单次上限改为前端可配。
  - 过滤 XML 标签内部的标记，防止思考过程（`<thinking>`）误触发网络搜索。
  - 修复联网补充回答后丢失思维链的问题。

## 0.4.3 · 2026-09-08
- **接入联网搜索**：模型回复 `[搜索:关键词]` 即可触发搜索（支持 DuckDuckGo、Tavily、Brave），并再次交由模型整理回复。

## 0.4.1 - 0.4.2 · 2026-09-08
- **聊天快捷指令**：支持 `/clear`（回退轮数）、`/del`（清空当前上下文）、`/provider`（切换服务商）、`/model`（切换模型）、`/help`（查看帮助）。
- **界面优化**：优化高清应用图标展示；前端代码进行模块化彻底解耦。

## 0.3.3 - 0.3.6 · 2026-09-08
- **环境与时间感知**：
  - 自动向输入前缀注入双方本地时间、法定节假日与工作日状态。
  - 时间支持「同城模式」与「异地模式」切换，节省 Token。
  - 支持官方天气 API（和风天气、WeatherAPI）与灾害预警；优化缓存策略至 2 小时。
  - 修复城市输入框在 React 渲染下的焦点丢失 Bug。

## 0.3.0 - 0.3.2 · 2026-09-07
- **提示词与结构化系统**：
  - 全面使用 XML 标签（`<Character>`、`<User>`、`<World_Info>` 等）隔离提示词层级。
  - 引入**预设管理**（顺序拖拽、生成参数配置、内置去思维链正则）。
  - 引入**世界书系统**（支持全局/角色绑定、关键词触发与递归匹配）。
  - 支持动态分隔符变量 `{{sep}}`。

## 0.2.0 · 2026-09-15
- **控制台登录与安全**：引入强制密码修改机制，登录凭据基于 scrypt+盐加密。
- **开箱体验与向导**：
  - 新增「快速配置卡片」，开箱即配收发消息；提供老用户直通通道。
  - 新增「Uranus 助手」悬浮气泡，随时检索界面功能与排障。
  - 内置线上/线下开箱即用的预设。
- **细粒度出网代理**：支持分路由代理控制（仅外网依赖走代理，局域网与 Photon 桥接直连）。
- **版本更新与规范**：后台新增「检查更新」按钮；精简开源文档与架构细节。

## 0.1.0 · 2026-09-07
- **首个正式基础版本**：
  - 支持 Photon 云端及 Mac 本地两种 iMessage 收发模式。
  - 多角色/多线路同时在线隔离，支持按服务商集中管理 API 密钥。
  - 对话流按分隔符切片并模拟自然打字延迟。
  - 基础富文本与媒体解析（图生文、语音转文字）。
  - 本地 JSON 化轻量存储架构（单条目单文件，无需引入数据库）。
  - 完整的 SSE 实时控制台、服务重启与冷热备份机制。